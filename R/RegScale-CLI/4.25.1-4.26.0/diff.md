# Comparing `tmp/RegScale_CLI-4.25.1-py3-none-any.whl.zip` & `tmp/RegScale_CLI-4.26.0-py3-none-any.whl.zip`

## zipinfo {}

```diff
@@ -1,94 +1,100 @@
-Zip file size: 230112 bytes, number of entries: 92
--rw-r--r--  2.0 unx    14485 b- defN 23-Apr-14 00:04 regscale.py
--rw-r--r--  2.0 unx       47 b- defN 23-Apr-14 00:04 app/__init__.py
--rw-r--r--  2.0 unx       73 b- defN 23-Apr-14 00:04 app/_version.py
--rw-r--r--  2.0 unx    14497 b- defN 23-Apr-14 00:04 app/api.py
--rw-r--r--  2.0 unx    13397 b- defN 23-Apr-14 00:04 app/application.py
--rw-r--r--  2.0 unx     1287 b- defN 23-Apr-14 00:04 app/logz.py
--rw-r--r--  2.0 unx        0 b- defN 23-Apr-14 00:04 app/commercial/__init__.py
--rw-r--r--  2.0 unx    16359 b- defN 23-Apr-14 00:04 app/commercial/ad.py
--rw-r--r--  2.0 unx    45796 b- defN 23-Apr-14 00:04 app/commercial/defender.py
--rw-r--r--  2.0 unx     8007 b- defN 23-Apr-14 00:04 app/commercial/jira.py
--rw-r--r--  2.0 unx    27240 b- defN 23-Apr-14 00:04 app/commercial/okta.py
--rw-r--r--  2.0 unx    29595 b- defN 23-Apr-14 00:04 app/commercial/qualys.py
--rw-r--r--  2.0 unx    11689 b- defN 23-Apr-14 00:04 app/commercial/servicenow.py
--rw-r--r--  2.0 unx    62786 b- defN 23-Apr-14 00:04 app/commercial/stig.py
--rw-r--r--  2.0 unx    22634 b- defN 23-Apr-14 00:04 app/commercial/tenable.py
--rw-r--r--  2.0 unx    56457 b- defN 23-Apr-14 00:04 app/commercial/wiz.py
--rw-r--r--  2.0 unx        0 b- defN 23-Apr-14 00:04 app/internal/__init__.py
--rw-r--r--  2.0 unx    26998 b- defN 23-Apr-14 00:04 app/internal/admin_actions.py
--rw-r--r--  2.0 unx    31042 b- defN 23-Apr-14 00:04 app/internal/assessments_editor.py
--rw-r--r--  2.0 unx    16114 b- defN 23-Apr-14 00:04 app/internal/comparison.py
--rw-r--r--  2.0 unx    15277 b- defN 23-Apr-14 00:04 app/internal/control_editor.py
--rw-r--r--  2.0 unx     5912 b- defN 23-Apr-14 00:04 app/internal/encrypt.py
--rw-r--r--  2.0 unx    40122 b- defN 23-Apr-14 00:04 app/internal/evidence.py
--rw-r--r--  2.0 unx     2325 b- defN 23-Apr-14 00:04 app/internal/healthcheck.py
--rw-r--r--  2.0 unx     6397 b- defN 23-Apr-14 00:04 app/internal/login.py
--rw-r--r--  2.0 unx     9419 b- defN 23-Apr-14 00:04 app/internal/migrations.py
--rw-r--r--  2.0 unx        0 b- defN 23-Apr-14 00:04 app/public/__init__.py
--rw-r--r--  2.0 unx    18601 b- defN 23-Apr-14 00:04 app/public/cisa.py
--rw-r--r--  2.0 unx    11186 b- defN 23-Apr-14 00:04 app/public/emass.py
--rw-r--r--  2.0 unx    63918 b- defN 23-Apr-14 00:04 app/public/fedramp.py
--rw-r--r--  2.0 unx     7946 b- defN 23-Apr-14 00:04 app/public/nist_catalog.py
--rw-r--r--  2.0 unx    71111 b- defN 23-Apr-14 00:04 app/public/oscal.py
--rw-r--r--  2.0 unx     6081 b- defN 23-Apr-14 00:04 app/public/otx.py
--rw-r--r--  2.0 unx        0 b- defN 23-Apr-14 00:04 app/utils/__init__.py
--rw-r--r--  2.0 unx    21592 b- defN 23-Apr-14 00:04 app/utils/app_utils.py
--rw-r--r--  2.0 unx    10409 b- defN 23-Apr-14 00:04 app/utils/regscale_utils.py
--rw-r--r--  2.0 unx     1591 b- defN 23-Apr-14 00:04 app/utils/threadhandler.py
--rw-r--r--  2.0 unx        0 b- defN 23-Apr-14 00:04 exceptions/__init__.py
--rw-r--r--  2.0 unx      195 b- defN 23-Apr-14 00:04 exceptions/license_exception.py
--rw-r--r--  2.0 unx       79 b- defN 23-Apr-14 00:04 models/__init__.py
--rw-r--r--  2.0 unx        0 b- defN 23-Apr-14 00:04 models/app_models/__init__.py
--rw-r--r--  2.0 unx     3997 b- defN 23-Apr-14 00:04 models/app_models/click.py
--rw-r--r--  2.0 unx    13863 b- defN 23-Apr-14 00:04 models/app_models/control_editor.py
--rw-r--r--  2.0 unx      889 b- defN 23-Apr-14 00:04 models/app_models/pipeline.py
--rw-r--r--  2.0 unx        0 b- defN 23-Apr-14 00:04 models/integration_models/__init__.py
--rw-r--r--  2.0 unx     7858 b- defN 23-Apr-14 00:04 models/integration_models/azure_alerts.py
--rw-r--r--  2.0 unx      872 b- defN 23-Apr-14 00:04 models/integration_models/recommendations.py
--rw-r--r--  2.0 unx     8319 b- defN 23-Apr-14 00:04 models/integration_models/tenable.py
--rw-r--r--  2.0 unx     1833 b- defN 23-Apr-14 00:04 models/integration_models/wiz.py
--rw-r--r--  2.0 unx        0 b- defN 23-Apr-14 00:04 models/regscale_models/__init__.py
--rw-r--r--  2.0 unx     6688 b- defN 23-Apr-14 00:04 models/regscale_models/assessment.py
--rw-r--r--  2.0 unx     7726 b- defN 23-Apr-14 00:04 models/regscale_models/asset.py
--rw-r--r--  2.0 unx     4327 b- defN 23-Apr-14 00:04 models/regscale_models/checklist.py
--rw-r--r--  2.0 unx     1877 b- defN 23-Apr-14 00:04 models/regscale_models/components.py
--rw-r--r--  2.0 unx    10070 b- defN 23-Apr-14 00:04 models/regscale_models/control_implementation.py
--rw-r--r--  2.0 unx     1441 b- defN 23-Apr-14 00:04 models/regscale_models/control_objective.py
--rw-r--r--  2.0 unx     6294 b- defN 23-Apr-14 00:04 models/regscale_models/implementation_objective.py
--rw-r--r--  2.0 unx     2981 b- defN 23-Apr-14 00:04 models/regscale_models/implementation_option.py
--rw-r--r--  2.0 unx     2850 b- defN 23-Apr-14 00:04 models/regscale_models/interconnects.py
--rw-r--r--  2.0 unx     4175 b- defN 23-Apr-14 00:04 models/regscale_models/issue.py
--rw-r--r--  2.0 unx     4878 b- defN 23-Apr-14 00:04 models/regscale_models/modules.py
--rw-r--r--  2.0 unx      232 b- defN 23-Apr-14 00:04 models/regscale_models/objective.py
--rw-r--r--  2.0 unx     2353 b- defN 23-Apr-14 00:04 models/regscale_models/ports_protocols.py
--rw-r--r--  2.0 unx     2415 b- defN 23-Apr-14 00:04 models/regscale_models/requirements.py
--rw-r--r--  2.0 unx     5886 b- defN 23-Apr-14 00:04 models/regscale_models/securityplans.py
--rw-r--r--  2.0 unx    25813 b- defN 23-Apr-14 00:04 models/regscale_models/stig.py
--rw-r--r--  2.0 unx     1425 b- defN 23-Apr-14 00:04 models/regscale_models/threat.py
--rw-r--r--  2.0 unx     2242 b- defN 23-Apr-14 00:04 models/regscale_models/user.py
--rw-r--r--  2.0 unx        0 b- defN 23-Apr-14 00:04 tests/__init__.py
--rw-r--r--  2.0 unx     1818 b- defN 23-Apr-14 00:04 tests/conftest.py
--rw-r--r--  2.0 unx      368 b- defN 23-Apr-14 00:04 tests/mock_api_service.py
--rw-r--r--  2.0 unx     1334 b- defN 23-Apr-14 00:04 tests/test_app_utils.py
--rw-r--r--  2.0 unx     1930 b- defN 23-Apr-14 00:04 tests/test_assessments_editor.py
--rw-r--r--  2.0 unx     1687 b- defN 23-Apr-14 00:04 tests/test_cisa.py
--rw-r--r--  2.0 unx     1747 b- defN 23-Apr-14 00:04 tests/test_control_editor.py
--rw-r--r--  2.0 unx     1425 b- defN 23-Apr-14 00:04 tests/test_dependabot.py
--rw-r--r--  2.0 unx     2827 b- defN 23-Apr-14 00:04 tests/test_emass.py
--rw-r--r--  2.0 unx    14137 b- defN 23-Apr-14 00:04 tests/test_evidence.py
--rw-r--r--  2.0 unx     1186 b- defN 23-Apr-14 00:04 tests/test_login.py
--rw-r--r--  2.0 unx     1416 b- defN 23-Apr-14 00:04 tests/test_npm_audit.py
--rw-r--r--  2.0 unx     9248 b- defN 23-Apr-14 00:04 tests/test_oscal.py
--rw-r--r--  2.0 unx      866 b- defN 23-Apr-14 00:04 tests/test_snow.py
--rw-r--r--  2.0 unx     1245 b- defN 23-Apr-14 00:04 tests/test_sonarcloud.py
--rw-r--r--  2.0 unx     1258 b- defN 23-Apr-14 00:04 tests/test_stig.py
--rw-r--r--  2.0 unx     1666 b- defN 23-Apr-14 00:04 tests/test_tenable.py
--rw-r--r--  2.0 unx     1254 b- defN 23-Apr-14 00:04 tests/test_update_regscale_config.py
--rw-r--r--  2.0 unx     1076 b- defN 23-Apr-14 00:05 RegScale_CLI-4.25.1.dist-info/LICENSE
--rw-r--r--  2.0 unx     6330 b- defN 23-Apr-14 00:05 RegScale_CLI-4.25.1.dist-info/METADATA
--rw-r--r--  2.0 unx       92 b- defN 23-Apr-14 00:05 RegScale_CLI-4.25.1.dist-info/WHEEL
--rw-r--r--  2.0 unx       43 b- defN 23-Apr-14 00:05 RegScale_CLI-4.25.1.dist-info/entry_points.txt
--rw-r--r--  2.0 unx       37 b- defN 23-Apr-14 00:05 RegScale_CLI-4.25.1.dist-info/top_level.txt
-?rw-rw-r--  2.0 unx     7715 b- defN 23-Apr-14 00:05 RegScale_CLI-4.25.1.dist-info/RECORD
-92 files, 882673 bytes uncompressed, 218010 bytes compressed:  75.3%
+Zip file size: 238688 bytes, number of entries: 98
+-rw-r--r--  2.0 unx    14585 b- defN 23-Apr-19 14:44 regscale.py
+-rw-r--r--  2.0 unx       47 b- defN 23-Apr-19 14:44 app/__init__.py
+-rw-r--r--  2.0 unx       73 b- defN 23-Apr-19 14:44 app/_version.py
+-rw-r--r--  2.0 unx    14497 b- defN 23-Apr-19 14:44 app/api.py
+-rw-r--r--  2.0 unx    13397 b- defN 23-Apr-19 14:44 app/application.py
+-rw-r--r--  2.0 unx     1287 b- defN 23-Apr-19 14:44 app/logz.py
+-rw-r--r--  2.0 unx        0 b- defN 23-Apr-19 14:44 app/commercial/__init__.py
+-rw-r--r--  2.0 unx    16359 b- defN 23-Apr-19 14:44 app/commercial/ad.py
+-rw-r--r--  2.0 unx    45796 b- defN 23-Apr-19 14:44 app/commercial/defender.py
+-rw-r--r--  2.0 unx     8007 b- defN 23-Apr-19 14:44 app/commercial/jira.py
+-rw-r--r--  2.0 unx    27240 b- defN 23-Apr-19 14:44 app/commercial/okta.py
+-rw-r--r--  2.0 unx    29595 b- defN 23-Apr-19 14:44 app/commercial/qualys.py
+-rw-r--r--  2.0 unx    11689 b- defN 23-Apr-19 14:44 app/commercial/servicenow.py
+-rw-r--r--  2.0 unx    62800 b- defN 23-Apr-19 14:44 app/commercial/stig.py
+-rw-r--r--  2.0 unx    22634 b- defN 23-Apr-19 14:44 app/commercial/tenable.py
+-rw-r--r--  2.0 unx    56457 b- defN 23-Apr-19 14:44 app/commercial/wiz.py
+-rw-r--r--  2.0 unx        0 b- defN 23-Apr-19 14:44 app/internal/__init__.py
+-rw-r--r--  2.0 unx    26998 b- defN 23-Apr-19 14:44 app/internal/admin_actions.py
+-rw-r--r--  2.0 unx    31042 b- defN 23-Apr-19 14:44 app/internal/assessments_editor.py
+-rw-r--r--  2.0 unx      910 b- defN 23-Apr-19 14:44 app/internal/catalog.py
+-rw-r--r--  2.0 unx    16114 b- defN 23-Apr-19 14:44 app/internal/comparison.py
+-rw-r--r--  2.0 unx    15277 b- defN 23-Apr-19 14:44 app/internal/control_editor.py
+-rw-r--r--  2.0 unx     5912 b- defN 23-Apr-19 14:44 app/internal/encrypt.py
+-rw-r--r--  2.0 unx    40122 b- defN 23-Apr-19 14:44 app/internal/evidence.py
+-rw-r--r--  2.0 unx     2325 b- defN 23-Apr-19 14:44 app/internal/healthcheck.py
+-rw-r--r--  2.0 unx     6471 b- defN 23-Apr-19 14:44 app/internal/login.py
+-rw-r--r--  2.0 unx     8731 b- defN 23-Apr-19 14:44 app/internal/migrations.py
+-rw-r--r--  2.0 unx        0 b- defN 23-Apr-19 14:44 app/public/__init__.py
+-rw-r--r--  2.0 unx    18601 b- defN 23-Apr-19 14:44 app/public/cisa.py
+-rw-r--r--  2.0 unx    11186 b- defN 23-Apr-19 14:44 app/public/emass.py
+-rw-r--r--  2.0 unx    63918 b- defN 23-Apr-19 14:44 app/public/fedramp.py
+-rw-r--r--  2.0 unx     7946 b- defN 23-Apr-19 14:44 app/public/nist_catalog.py
+-rw-r--r--  2.0 unx    72156 b- defN 23-Apr-19 14:44 app/public/oscal.py
+-rw-r--r--  2.0 unx     6081 b- defN 23-Apr-19 14:44 app/public/otx.py
+-rw-r--r--  2.0 unx        0 b- defN 23-Apr-19 14:44 app/utils/__init__.py
+-rw-r--r--  2.0 unx    22103 b- defN 23-Apr-19 14:44 app/utils/app_utils.py
+-rw-r--r--  2.0 unx    10409 b- defN 23-Apr-19 14:44 app/utils/regscale_utils.py
+-rw-r--r--  2.0 unx     1591 b- defN 23-Apr-19 14:44 app/utils/threadhandler.py
+-rw-r--r--  2.0 unx        0 b- defN 23-Apr-19 14:44 app/utils/catalog_utils/__init__.py
+-rw-r--r--  2.0 unx     7343 b- defN 23-Apr-19 14:44 app/utils/catalog_utils/compare_catalog.py
+-rw-r--r--  2.0 unx     4491 b- defN 23-Apr-19 14:44 app/utils/catalog_utils/diagnostic_catalog.py
+-rw-r--r--  2.0 unx     2653 b- defN 23-Apr-19 14:44 app/utils/catalog_utils/export_catalog.py
+-rw-r--r--  2.0 unx        0 b- defN 23-Apr-19 14:44 exceptions/__init__.py
+-rw-r--r--  2.0 unx      195 b- defN 23-Apr-19 14:44 exceptions/license_exception.py
+-rw-r--r--  2.0 unx       79 b- defN 23-Apr-19 14:44 models/__init__.py
+-rw-r--r--  2.0 unx        0 b- defN 23-Apr-19 14:44 models/app_models/__init__.py
+-rw-r--r--  2.0 unx     6556 b- defN 23-Apr-19 14:44 models/app_models/catalog_compare.py
+-rw-r--r--  2.0 unx     3997 b- defN 23-Apr-19 14:44 models/app_models/click.py
+-rw-r--r--  2.0 unx    13863 b- defN 23-Apr-19 14:44 models/app_models/control_editor.py
+-rw-r--r--  2.0 unx      889 b- defN 23-Apr-19 14:44 models/app_models/pipeline.py
+-rw-r--r--  2.0 unx        0 b- defN 23-Apr-19 14:44 models/integration_models/__init__.py
+-rw-r--r--  2.0 unx     7858 b- defN 23-Apr-19 14:44 models/integration_models/azure_alerts.py
+-rw-r--r--  2.0 unx      872 b- defN 23-Apr-19 14:44 models/integration_models/recommendations.py
+-rw-r--r--  2.0 unx     8319 b- defN 23-Apr-19 14:44 models/integration_models/tenable.py
+-rw-r--r--  2.0 unx     1833 b- defN 23-Apr-19 14:44 models/integration_models/wiz.py
+-rw-r--r--  2.0 unx        0 b- defN 23-Apr-19 14:44 models/regscale_models/__init__.py
+-rw-r--r--  2.0 unx     6688 b- defN 23-Apr-19 14:44 models/regscale_models/assessment.py
+-rw-r--r--  2.0 unx     7726 b- defN 23-Apr-19 14:44 models/regscale_models/asset.py
+-rw-r--r--  2.0 unx     4327 b- defN 23-Apr-19 14:44 models/regscale_models/checklist.py
+-rw-r--r--  2.0 unx     1877 b- defN 23-Apr-19 14:44 models/regscale_models/components.py
+-rw-r--r--  2.0 unx    10070 b- defN 23-Apr-19 14:44 models/regscale_models/control_implementation.py
+-rw-r--r--  2.0 unx     1441 b- defN 23-Apr-19 14:44 models/regscale_models/control_objective.py
+-rw-r--r--  2.0 unx     6294 b- defN 23-Apr-19 14:44 models/regscale_models/implementation_objective.py
+-rw-r--r--  2.0 unx     2981 b- defN 23-Apr-19 14:44 models/regscale_models/implementation_option.py
+-rw-r--r--  2.0 unx     2850 b- defN 23-Apr-19 14:44 models/regscale_models/interconnects.py
+-rw-r--r--  2.0 unx     4175 b- defN 23-Apr-19 14:44 models/regscale_models/issue.py
+-rw-r--r--  2.0 unx     4878 b- defN 23-Apr-19 14:44 models/regscale_models/modules.py
+-rw-r--r--  2.0 unx      232 b- defN 23-Apr-19 14:44 models/regscale_models/objective.py
+-rw-r--r--  2.0 unx     2353 b- defN 23-Apr-19 14:44 models/regscale_models/ports_protocols.py
+-rw-r--r--  2.0 unx     2415 b- defN 23-Apr-19 14:44 models/regscale_models/requirements.py
+-rw-r--r--  2.0 unx     5949 b- defN 23-Apr-19 14:44 models/regscale_models/security_control.py
+-rw-r--r--  2.0 unx     5886 b- defN 23-Apr-19 14:44 models/regscale_models/securityplans.py
+-rw-r--r--  2.0 unx    25813 b- defN 23-Apr-19 14:44 models/regscale_models/stig.py
+-rw-r--r--  2.0 unx     1425 b- defN 23-Apr-19 14:44 models/regscale_models/threat.py
+-rw-r--r--  2.0 unx     2242 b- defN 23-Apr-19 14:44 models/regscale_models/user.py
+-rw-r--r--  2.0 unx        0 b- defN 23-Apr-19 14:44 tests/__init__.py
+-rw-r--r--  2.0 unx     1818 b- defN 23-Apr-19 14:44 tests/conftest.py
+-rw-r--r--  2.0 unx     1334 b- defN 23-Apr-19 14:44 tests/test_app_utils.py
+-rw-r--r--  2.0 unx     1930 b- defN 23-Apr-19 14:44 tests/test_assessments_editor.py
+-rw-r--r--  2.0 unx     1687 b- defN 23-Apr-19 14:44 tests/test_cisa.py
+-rw-r--r--  2.0 unx     1747 b- defN 23-Apr-19 14:44 tests/test_control_editor.py
+-rw-r--r--  2.0 unx     1425 b- defN 23-Apr-19 14:44 tests/test_dependabot.py
+-rw-r--r--  2.0 unx     2827 b- defN 23-Apr-19 14:44 tests/test_emass.py
+-rw-r--r--  2.0 unx    14091 b- defN 23-Apr-19 14:44 tests/test_evidence.py
+-rw-r--r--  2.0 unx     1186 b- defN 23-Apr-19 14:44 tests/test_login.py
+-rw-r--r--  2.0 unx     1416 b- defN 23-Apr-19 14:44 tests/test_npm_audit.py
+-rw-r--r--  2.0 unx     9248 b- defN 23-Apr-19 14:44 tests/test_oscal.py
+-rw-r--r--  2.0 unx      866 b- defN 23-Apr-19 14:44 tests/test_snow.py
+-rw-r--r--  2.0 unx     1245 b- defN 23-Apr-19 14:44 tests/test_sonarcloud.py
+-rw-r--r--  2.0 unx     1258 b- defN 23-Apr-19 14:44 tests/test_stig.py
+-rw-r--r--  2.0 unx     1666 b- defN 23-Apr-19 14:44 tests/test_tenable.py
+-rw-r--r--  2.0 unx     1254 b- defN 23-Apr-19 14:44 tests/test_update_regscale_config.py
+-rw-r--r--  2.0 unx     1076 b- defN 23-Apr-19 14:44 RegScale_CLI-4.26.0.dist-info/LICENSE
+-rw-r--r--  2.0 unx     6330 b- defN 23-Apr-19 14:44 RegScale_CLI-4.26.0.dist-info/METADATA
+-rw-r--r--  2.0 unx       92 b- defN 23-Apr-19 14:44 RegScale_CLI-4.26.0.dist-info/WHEEL
+-rw-r--r--  2.0 unx       43 b- defN 23-Apr-19 14:44 RegScale_CLI-4.26.0.dist-info/entry_points.txt
+-rw-r--r--  2.0 unx       37 b- defN 23-Apr-19 14:44 RegScale_CLI-4.26.0.dist-info/top_level.txt
+?rw-rw-r--  2.0 unx     8293 b- defN 23-Apr-19 14:44 RegScale_CLI-4.26.0.dist-info/RECORD
+98 files, 911795 bytes uncompressed, 225652 bytes compressed:  75.3%
```

## zipnote {}

```diff
@@ -51,14 +51,17 @@
 
 Filename: app/internal/admin_actions.py
 Comment: 
 
 Filename: app/internal/assessments_editor.py
 Comment: 
 
+Filename: app/internal/catalog.py
+Comment: 
+
 Filename: app/internal/comparison.py
 Comment: 
 
 Filename: app/internal/control_editor.py
 Comment: 
 
 Filename: app/internal/encrypt.py
@@ -105,26 +108,41 @@
 
 Filename: app/utils/regscale_utils.py
 Comment: 
 
 Filename: app/utils/threadhandler.py
 Comment: 
 
+Filename: app/utils/catalog_utils/__init__.py
+Comment: 
+
+Filename: app/utils/catalog_utils/compare_catalog.py
+Comment: 
+
+Filename: app/utils/catalog_utils/diagnostic_catalog.py
+Comment: 
+
+Filename: app/utils/catalog_utils/export_catalog.py
+Comment: 
+
 Filename: exceptions/__init__.py
 Comment: 
 
 Filename: exceptions/license_exception.py
 Comment: 
 
 Filename: models/__init__.py
 Comment: 
 
 Filename: models/app_models/__init__.py
 Comment: 
 
+Filename: models/app_models/catalog_compare.py
+Comment: 
+
 Filename: models/app_models/click.py
 Comment: 
 
 Filename: models/app_models/control_editor.py
 Comment: 
 
 Filename: models/app_models/pipeline.py
@@ -186,14 +204,17 @@
 
 Filename: models/regscale_models/ports_protocols.py
 Comment: 
 
 Filename: models/regscale_models/requirements.py
 Comment: 
 
+Filename: models/regscale_models/security_control.py
+Comment: 
+
 Filename: models/regscale_models/securityplans.py
 Comment: 
 
 Filename: models/regscale_models/stig.py
 Comment: 
 
 Filename: models/regscale_models/threat.py
@@ -204,17 +225,14 @@
 
 Filename: tests/__init__.py
 Comment: 
 
 Filename: tests/conftest.py
 Comment: 
 
-Filename: tests/mock_api_service.py
-Comment: 
-
 Filename: tests/test_app_utils.py
 Comment: 
 
 Filename: tests/test_assessments_editor.py
 Comment: 
 
 Filename: tests/test_cisa.py
@@ -252,26 +270,26 @@
 
 Filename: tests/test_tenable.py
 Comment: 
 
 Filename: tests/test_update_regscale_config.py
 Comment: 
 
-Filename: RegScale_CLI-4.25.1.dist-info/LICENSE
+Filename: RegScale_CLI-4.26.0.dist-info/LICENSE
 Comment: 
 
-Filename: RegScale_CLI-4.25.1.dist-info/METADATA
+Filename: RegScale_CLI-4.26.0.dist-info/METADATA
 Comment: 
 
-Filename: RegScale_CLI-4.25.1.dist-info/WHEEL
+Filename: RegScale_CLI-4.26.0.dist-info/WHEEL
 Comment: 
 
-Filename: RegScale_CLI-4.25.1.dist-info/entry_points.txt
+Filename: RegScale_CLI-4.26.0.dist-info/entry_points.txt
 Comment: 
 
-Filename: RegScale_CLI-4.25.1.dist-info/top_level.txt
+Filename: RegScale_CLI-4.26.0.dist-info/top_level.txt
 Comment: 
 
-Filename: RegScale_CLI-4.25.1.dist-info/RECORD
+Filename: RegScale_CLI-4.26.0.dist-info/RECORD
 Comment: 
 
 Zip file comment:
```

## regscale.py

```diff
@@ -45,14 +45,15 @@
 ############################################################
 import app.internal.healthcheck as hc
 import app.internal.login as lg
 
 from app.internal.admin_actions import actions
 from app.internal.comparison import compare
 from app.internal.assessments_editor import assessments
+from app.internal.catalog import catalog
 from app.internal.encrypt import JH0847, IOA21H98, YO9322
 from app.internal.evidence import evidence
 from app.internal.migrations import migrations
 from app.internal.control_editor import control_editor
 
 ############################################################
 # Public Integrations
@@ -439,13 +440,16 @@
 
 # add Control Editor Feature
 cli.add_command(control_editor)
 
 # add Assessments Editor Feature
 cli.add_command(assessments)
 
+# add Catalog Management Feature
+cli.add_command(catalog)
+
 # add Alienvault OTX integration
 cli.add_command(alienvault)
 
 # start function for the CLI
 if __name__ == "__main__":
     cli()
```

## app/_version.py

```diff
@@ -1,4 +1,4 @@
 #!/usr/bin/env python3
 # standard python imports
 
-__version__ = "4.25.1"
+__version__ = "4.26.0"
```

## app/commercial/stig.py

```diff
@@ -296,15 +296,15 @@
         # download cci html file
         mapping_url = "https://csrc.nist.gov/csrc/media/projects/forum/documents/stig-mapping-to-nist-800-53.xlsx"
         xl_path.touch()
         download_file(url=mapping_url, download_path=str(Path(save_path).parent))
     if not Path(save_path).exists() or force:
         url = "https://dl.dod.cyber.mil/wp-content/uploads/stigs/zip/U_CCI_List.zip"
         # download cci html file
-        file = download_file(url=url, download_path=tempfile.gettempdir())
+        file = download_file(url=url, download_path=tempfile.gettempdir(), verify=False)
         with zipfile.ZipFile(file, "r") as zip_ref:
             zip_ref.extractall(tempfile.gettempdir())
             shutil.copy(dl_path, save_path)
     if not (Path("assets") / "data_cci.json").exists() or force:
         mapping = update_mapping()
     else:
         with open((Path("assets") / "data_cci.json"), encoding="utf-8") as json_file:
```

## app/internal/login.py

```diff
@@ -59,15 +59,16 @@
             # update init file from login
             config["token"] = token
             config["userId"] = user_id
             # write the changes back to file
             app.save_config(config)
             # set variables
             logger.info("User ID: %s", user_id)
-            logger.info("New RegScale Token: %s", token)
+            logger.info("New RegScale Token has been updated and saved in init.yaml")
+            logger.debug("Token: %s", token)
         except TypeError as ex:
             logger.error("TypeError: %s", ex)
         except SSLCertVerificationError as sslex:
             logger.error(
                 "SSLError, python requests requires a valid ssl certificate.\n%s", sslex
             )
             sys.exit(1)
```

## app/internal/migrations.py

```diff
@@ -1,25 +1,25 @@
 #!/usr/bin/env python3
 # -*- coding: utf-8 -*-
 """Performs data processing for bulk processing"""
 
 # standard python imports
+import json
 from os import sep
 from typing import Tuple
 
 import click
 import requests
-import json
+
 from app.api import Api
 from app.application import Application
 from app.logz import create_logger
 from app.utils.app_utils import (
     check_file_path,
     create_progress_object,
-    error_and_exit,
     save_data_to,
 )
 from app.utils.regscale_utils import get_all_from_module
 from app.utils.threadhandler import create_threads, thread_assignment
 
 app = Application()
 config = app.config
@@ -197,53 +197,41 @@
 
 
 def initialize_and_fetch_data(module: str) -> Tuple[Api, list[dict]]:
     """
     Function to start application, api, and fetches all records for the provided module
     from RegScale via API and saves the output to a .json file
     :param str module: python module
-    :raises: General Error if token or domain are missing or blank in init.yaml
     :return: Tuple[Api object, list of data of provided module from RegScale API]
     :rtype: Tuple[Api, list[dict]]
     """
     # load the config from YAML
     app = Application()
     api = Api(app)
-    config = app.load_config()
 
-    # make sure config is set before processing
-    if config["domain"] is None:
-        error_and_exit("No domain set in the initialization file.")
-    elif config["domain"] == "":
-        error_and_exit("The domain is blank in the initialization file.")
-    elif config["token"] is None:
-        error_and_exit("The token has not been set in the initialization file.")
-    elif config["token"] == "":
-        error_and_exit("The token is blank in the initialization file.")
-    else:
-        # get the data of provided module from RegScale via API
-        regscale_data = get_all_from_module(api=api, module=module)
-
-        # verify artifacts folder exists
-        check_file_path("artifacts")
-
-        # write out risks data to file
-        save_data_to(
-            file_name=f"artifacts{sep}RegScale{module.title()}",
-            file_type=".json",
-            data=regscale_data,
-        )
-        logger.info(
-            "Writing out RegScale risk list to the artifacts folder (see RegScale%sList.json).",
-            module.title(),
-        )
-        logger.info(
-            "%s %s retrieved for processing from RegScale.", len(regscale_data), module
-        )
-        return api, regscale_data
+    # get the data of provided module from RegScale via API
+    regscale_data = get_all_from_module(api=api, module=module)
+
+    # verify artifacts folder exists
+    check_file_path("artifacts")
+
+    # write out risks data to file
+    save_data_to(
+        file_name=f"artifacts{sep}RegScale{module.title()}",
+        file_type=".json",
+        data=regscale_data,
+    )
+    logger.info(
+        "Writing out RegScale risk list to the artifacts folder (see RegScale%sList.json).",
+        module.title(),
+    )
+    logger.info(
+        "%s %s retrieved for processing from RegScale.", len(regscale_data), module
+    )
+    return api, regscale_data
 
 
 def process_data(args: Tuple, thread: int) -> None:
     """
     Function to utilize threading and process the data from RegScale
     :param Tuple args: Tuple of args to use during the process
     :param int thread: Thread number of current thread
```

## app/public/oscal.py

```diff
@@ -6,15 +6,14 @@
 import dataclasses
 import json
 import re
 import tempfile
 import uuid
 from os import remove, sep
 from pathlib import Path
-from subprocess import CalledProcessError, run
 from typing import Tuple
 
 import click
 import pandas as pd
 import requests
 import xmltodict
 import yaml
@@ -28,26 +27,28 @@
     check_file_path,
     create_progress_object,
     error_and_exit,
     find_keys,
     get_file_name,
     reformat_str_date,
     save_data_to,
+    check_url,
 )
 from app.utils.threadhandler import create_threads, thread_assignment
+from models.regscale_models.security_control import SecurityControl
 from models.regscale_models.components import Component
 from models.regscale_models.control_implementation import ControlImplementation
 
 # create global variables
 job_progress = create_progress_object()
 logger = create_logger()
 app = Application()
 config = app.config
 api = Api(app)
-obj_to_controls = False
+OBJ_TO_CONTROLS = False
 
 # update api pool limits to maxThread count from init.yaml
 api.pool_connections = (
     config["maxThreads"]
     if config["maxThreads"] > api.pool_connections
     else api.pool_connections
 )
@@ -73,15 +74,14 @@
 
 
 # OSCAL Version Support
 @oscal.command()
 def version():
     """Info on current OSCAL version supported by RegScale."""
     logger.info("RegScale currently supports OSCAL Version 1.0.")
-    check_oscal()
 
 
 def convert_oscal_comp_to_regscale(j_data: dict) -> None:
     """
     Convert OSCAL component dict into a RegScale Component
     :param dict j_data: OSCAL component
     :raises: KeyError if components key isn't found in j_data
@@ -93,70 +93,75 @@
     component_id = None
     regscale_components = []
     components = []
     existing_components = api.get(
         url=config["domain"] + "/api/components/getList"
     ).json()
     controls_to_be_added = []
-    j_data["component-definition"]["metadata"]
+
     try:
         components = j_data["component-definition"]["components"]
     except KeyError as kex:
         error_and_exit(f"Key Error! {kex}")
 
     for comp in components:
         control_implementations = comp["control-implementations"]
+
         base_catalog = ""
-        try:
-            base_catalog = api.get(
-                url=comp["control-implementations"][0]["source"], headers={}
-            ).json()["catalog"]["metadata"]["title"]
-        except requests.exceptions.RequestException as rex:
-            logger.error(rex)
-        except KeyError as kex:
-            logger.error(kex)
+        title = ""
+
+        # alternative
+        base_catalog = comp["control-implementations"][0]["source"]
+
         component = Component(
-            title=comp["title"],
+            title=comp.get("title", ""),
             componentOwnerId=app.config["userId"],
-            componentType=comp["type"].lower(),
-            description=comp["description"],
-            purpose=comp["purpose"],
+            componentType=comp.get("type", "").lower(),
+            description=comp.get("description", ""),
+            purpose=comp.get("purpose", ""),
         )
         regscale_components.append(dataclasses.asdict(component))
         for control_implements in control_implementations:
-            for control_data in control_implements["implemented-requirements"]:
-                d = {
-                    "title": control_data["control-id"],
-                    "description": control_data["description"],
-                }
-                controls_to_be_added.append(d)
+            if "implemented-requirements" in control_implements:
+                for control_data in control_implements["implemented-requirements"]:
+                    control_data = {
+                        "component": comp["title"],
+                        # "id": control_data["control-id"],
+                        "title": control_data["control-id"],
+                        "description": control_data["description"],
+                    }
+                    controls_to_be_added.append(control_data)
+            elif base_catalog and "implemented-requirements" not in control_implements:
+                # Get control data from base catalog
+                logger.debug("base_catalog: %s\ntitle: %s", base_catalog, title)
+
     for reg in regscale_components:
         check_component = [x for x in existing_components if x["title"] == reg["title"]]
         if not check_component:
-            r = api.post(url=f'{config["domain"]}/api/components/', json=reg)
-            if not r.raise_for_status():
-                component_id = r.json()["id"]
+            response = api.post(url=f'{config["domain"]}/api/components/', json=reg)
+            if not response.raise_for_status():
+                component_id = response.json()["id"]
                 logger.info("Successfully posted %s to RegScale.", reg["title"])
         else:
             for cmp in check_component:
-                r = api.put(
+                response = api.put(
                     url=f'{config["domain"]}/api/components/{cmp["id"]}', json=reg
                 )
-                if not r.raise_for_status():
+                if not response.raise_for_status():
                     component_id = cmp["id"]
                     # component_id = r.json()['id']
                     logger.info(
                         "Successfully updated component %s in RegScale.", cmp["title"]
                     )
-    # Load controls to RegScale and associate with new component
-    load_controls(
-        controls_to_be_added=controls_to_be_added,
-        component_id=component_id,
-        base_catalog=base_catalog,
-    )
+        # Load controls to RegScale and associate with new component
+        load_controls(
+            controls_to_be_added=controls_to_be_added,
+            component_id=component_id,
+            base_catalog=base_catalog,
+        )
 
 
 @oscal.command(name="component")
 @click.argument("file_name", type=click.Path(exists=True))
 def upload_component(file_name: str) -> None:
     """Upload OSCAL Component to RegScale."""
     # Load Controls and assign to component
@@ -175,33 +180,35 @@
     :return: None
     :rtype: None
     """
     control_implementations_to_insert = []
     control_implementations_to_update = []
     try:
         imp_r = api.get(
-            url=f'{config["domain"]}/api/controlImplementation/getAllByParent/{component_id}/components'
+            url=f'{config["domain"]}/api/controlImplementation/getAllByParent/{component_id}\
+                /components'
         )
         if not imp_r.raise_for_status():
             all_implementations = imp_r.json()
-    except requests.RequestException as rex:
+    except requests.RequestException:
         logger.debug("No control implementations found for %s.", component_id)
         all_implementations = []
     cats = api.get(url=f'{config["domain"]}/api/catalogues/getList').json()
     cat = [cat for cat in cats if cat["title"] == base_catalog]
 
     if len(cat) > 0:
         cat_id = cat[0]["id"]
         try:
             controls = api.get(
                 f'{config["domain"]}/api/SecurityControls/getList/{cat_id}'
             ).json()
         except requests.RequestException as rex:
             error_and_exit(rex)
         for imp_control in controls_to_be_added:
+            logger.debug("imp_control: %s", imp_control)
             try:
                 reg_control = [
                     control
                     for control in controls
                     if control["controlId"].lower() == imp_control["title"].lower()
                 ]
                 if len(reg_control) > 0:
@@ -210,33 +217,44 @@
                         parentModule="components",
                         controlOwnerId=config["userId"],
                         status="Fully Implemented",
                         controlID=reg_control[0]["id"],
                         implementation=imp_control["description"],
                     )
                     dat = dataclasses.asdict(control_implementation)
-                    if control_implementation.implementation not in [
-                        imp["implementation"]
+                    control = SecurityControl.lookup_control(
+                        app=app, control_id=dat["controlID"]
+                    )
+                    if control.controlId not in [
+                        imp["controlName"]
                         for imp in all_implementations
-                        if imp["implementation"]
-                        == control_implementation.implementation
+                        if imp["parentId"] == control_implementation.parentId
                     ]:
                         control_implementations_to_insert.append(dat)
                     else:
-                        dat["id"] = [
-                            imp["id"]
+                        dat = [
+                            imp
                             for imp in all_implementations
-                            if imp["implementation"] == dat["implementation"]
+                            if imp["controlName"] == control.controlId
+                            and imp["parentId"] == dat["parentId"]
                         ][0]
+                        dat["implementation"] = control_implementation.implementation
+                        dat["status"] = control_implementation.status
                         control_implementations_to_update.append(dat)
             except requests.RequestException as rex:
                 logger.error(rex)
             except IndexError as iex:
                 logger.error("Index Error: %s\n%s", dat, iex)
     logger.debug(control_implementations_to_insert)
+    logger.debug("Controls to insert: %i", len(control_implementations_to_insert))
+    logger.debug("Controls to update: %i", len(control_implementations_to_update))
+    logger.debug(
+        "control_implementations_to_insert\n%s", control_implementations_to_insert
+    )
+
     # Insert or update new control implementations
     if control_implementations_to_insert:
         api.update_server(
             method="post",
             url=f'{config["domain"]}/api/controlImplementation',
             message=f"Inserting {len(control_implementations_to_insert)} implementations...",
             json_list=control_implementations_to_insert,
@@ -275,14 +293,16 @@
     :param str input: Path to the original file to convert
     :param str output: Desired path of the converted file
     :return: None
     :rtype: None
     """
     # Create object
     with open(input, "r") as file_in, open(output, "w") as file_out:
+        if Path(input).suffix == ".json":
+            obj = json.load(file_in)
         if Path(input).suffix == ".xml":
             obj = xmltodict.parse((file_in.read()))
         if Path(input).suffix in [".yaml", ".yml"]:
             obj = yaml.safe_load(file_in.read())
         json.dump(obj, file_out)
 
 
@@ -395,15 +415,16 @@
         # get the list of existing controls for the catalog
         url_sc = f'{config["domain"]}/api/SecurityControls/getList/{catalog}'
         try:
             sc_response = api.get(url_sc, headers=headers)
             sc_data = sc_response.json()
         except requests.exceptions.RequestException as ex:
             error_and_exit(
-                f"Unable to retrieve security controls for this catalogue in RegScale.\nError: {ex}\n{sc_response.text}",
+                f"Unable to retrieve security controls for this catalogue in RegScale.\nError: \
+                    {ex}\n{sc_response.text}",
             )
 
         # loop through each item in the OSCAL control set
         mappings = []
         for m in oscal_data["profile"]["imports"][0]["include-controls"][0]["with-ids"]:
             b_match = False
             for sc in sc_data:
@@ -434,15 +455,16 @@
 
 
 # Process catalog from OSCAL
 @oscal.command()
 @click.option(
     "--file_name",
     type=click.Path(exists=True, dir_okay=False, file_okay=True),
-    help="RegScale will process and load the catalogue along with all controls, statements, and parameters.",
+    help="RegScale will process and load the catalogue along with all controls, statements, and \
+        parameters.",
     prompt="Enter the file name of catalogue in NIST OSCAL to process",
     required=True,
 )
 @click.option(
     "--obj_to_controls",
     type=click.BOOL,
     default=0,
@@ -995,15 +1017,15 @@
         # create each security control
 
         description = str_parts.replace(
             "<ul><li>{{" + control["id"] + "_smt}} - </li><ul>",
             "<ul><li>{{" + control["id"] + "_smt}} - Control:</li><ul>",
         )
 
-        sc = {
+        security_control = {
             "title": control["id"] + " - " + control["title"],
             "controlType": "Stand-Alone",
             "controlId": control["id"],
             "description": description + control["guidance"],
             "references": control["links"],
             "relatedControls": "",
             "subControls": "",
@@ -1017,30 +1039,32 @@
             "createdById": config["userId"],
             "lastUpdatedById": config["userId"],
         }
         # attempt to create the security control
         if upload_flag:
             try:
                 # upload to RegScale
-                response = api.post(url=url_sc, json=sc)
+                response = api.post(url=url_sc, json=security_control)
                 json_response = response.json()
-                logger.debug("\n\nSuccess - %s", sc["title"])
+                logger.debug("\n\nSuccess - %s", security_control["title"])
 
                 # update id to the new control id
-                sc["id"] = json_response["id"]
-                new_controls.append(sc)
+                security_control["id"] = json_response["id"]
+                new_controls.append(security_control)
 
                 # add the new controls
                 new_regscale_controls.append(json_response)
             except requests.exceptions.RequestException:
-                logger.error("Unable to create security control %s,", sc["title"])
-                errors.append(sc)
+                logger.error(
+                    "Unable to create security control %s,", security_control["title"]
+                )
+                errors.append(security_control)
         else:
             # append the result
-            new_regscale_controls.append(sc)
+            new_regscale_controls.append(security_control)
         job_progress.update(task, advance=1)
 
 
 def reformat_title(string: str):
     # strip everything before 'obj'
     new_string = None
     try:
@@ -1071,16 +1095,16 @@
     for elem in soup.find_all(elem):
         txt = elem.text.strip()
         result["clean_text"] = txt
         match = re.search(pattern, txt)
         if match:
             key = match.group(1)
             result["key"] = key
-        ix = txt.find(" -")
-        result["extract_text"] = txt[ix:]
+        dat_ix = txt.find(" -")
+        result["extract_text"] = txt[dat_ix:]
     return result
 
 
 def strip_tag(html_string: str, sub_str_to_find: str) -> str:
     """
     Function to strip an HTML tag from a string based on a substring
     :param str html_string: A string of HTML
@@ -1094,18 +1118,18 @@
         for tag in soup.find_all(
             lambda tag: tag.name == "li" and sub_str_to_find in tag.text
         ):
             tag.extract()
         # Strip empty HTML tags
         for tag in soup.find_all(lambda tag: not tag.contents or tag.contents == [""]):
             tag.extract()
-        for x in soup.find_all():
-            if len(x.get_text(strip=True)) == 0:
+        for datx in soup.find_all():
+            if len(datx.get_text(strip=True)) == 0:
                 # Remove empty tags
-                x.extract()
+                datx.extract()
         result = str(soup)
     except ValueError as vex:
         logger.error(vex)
     return result
 
 
 def post_alternative_controls(args: Tuple, thread: int) -> None:
@@ -1132,15 +1156,15 @@
         title = reformat_title(obj["name"])
         label = [part["part_label"] for part in parts if part["name"] == obj["name"]][0]
         # Create a Child Control for this special case
         security_control_id = obj["securityControlId"]
         ctrl_lookup = api.get(
             url=config["domain"] + f"/api/SecurityControls/{security_control_id}"
         ).json()
-        sc = {
+        security_control = {
             "title": f"objective: {label} - {obj['description']}"
             if title
             else "objective",  # f"{ctrl_lookup['controlId']} objective: {title}",
             "controlType": "Mapping",  # Can also be Stand-Alone
             "controlId": ctrl_lookup["controlId"],
             "description": obj["description"],
             "references": ctrl_lookup["references"],
@@ -1153,15 +1177,15 @@
             "weight": 0,
             "practiceLevel": "",
             "catalogueID": ctrl_lookup["catalogueID"],
             "createdById": config["userId"],
             "lastUpdatedById": config["userId"],
         }
         url_sc = config["domain"] + "/api/securitycontrols/"
-        api.post(url=url_sc, json=sc)
+        api.post(url=url_sc, json=security_control)
         job_progress.update(task, advance=1)
 
 
 def reformat_title(string: str):
     # strip everything before 'obj'
     new_string = None
     try:
@@ -1197,15 +1221,15 @@
         title = reformat_title(obj["name"])
         label = [part["part_label"] for part in parts if part["name"] == obj["name"]][0]
         # Create a Child Control for this special case
         security_control_id = obj["securityControlId"]
         ctrl_lookup = api.get(
             url=config["domain"] + f"/api/SecurityControls/{security_control_id}"
         ).json()
-        sc = {
+        security_control = {
             "title": f"objective: {label} - {obj['description']}"
             if title
             else "objective",  # f"{ctrl_lookup['controlId']} objective: {title}",
             "controlType": "Mapping",  # Can also be Stand-Alone
             "controlId": ctrl_lookup["controlId"],
             "description": obj["description"],
             "references": ctrl_lookup["references"],
@@ -1218,21 +1242,22 @@
             "weight": 0,
             "practiceLevel": "",
             "catalogueID": ctrl_lookup["catalogueID"],
             "createdById": config["userId"],
             "lastUpdatedById": config["userId"],
         }
         url_sc = config["domain"] + "/api/securitycontrols/"
-        api.post(url=url_sc, json=sc)
+        api.post(url=url_sc, json=security_control)
         job_progress.update(task, advance=1)
 
 
 def post_child_controls(args: Tuple, thread: int) -> None:
     """
-    Function to analyze child controls from OSCAL catalog and posts them to RegScale while using threads
+    Function to analyze child controls from OSCAL catalog and posts them to RegScale while using \
+        threads
     :param Tuple args: Tuple of args to use during the process
     :param int thread: Thread number of current thread
     :raises: requests.exceptions.RequestException if API call experienced an error
     :return: None
     :rtype: None
     """
     # set up local variables from args passed
@@ -1398,15 +1423,15 @@
     ) = args
 
     # set up RegScale URL
     url_objs = config["domain"] + "/api/controlObjectives/"
 
     # find which records should be executed by the current thread
 
-    obj_parts = [obj for obj in objectives]
+    obj_parts = list(objectives)
     threads = thread_assignment(thread=thread, total_items=len(obj_parts))
 
     # iterate through the thread assignment items and process them
     for i in range(len(threads)):
         # set the recommendation for the thread for later use in the function
         objective = obj_parts[threads[i]]
 
@@ -1534,56 +1559,56 @@
                             "Unable to update parent objective: %s.",
                             update_parent["name"],
                         )
                         errors.append(update_parent)
         job_progress.update(task, advance=1)
 
 
-def process_objectives(obj, parts, ctrl, parent_id) -> str:
+def process_objectives(objs, parts, ctrl, parent_id) -> str:
     """
     Function for recursively working through objectives and formatting it as HTML description
     :param obj: RegScale object
     :param parts: Parts of the object
     :param ctrl: Controls for the object
     :param parent_id: Parent id of the Object
     :return: HTML formatted string of object
     :rtype: str
     """
     str_obj = "<ul>"
     # loop through parts/objectives recursively
-    for o in obj:
+    for obj in objs:
         # check prose
         str_prose = ""
-        if "prose" in o:
-            str_prose = o["prose"]
+        if "prose" in obj:
+            str_prose = obj["prose"]
 
         # check name
         str_name = ""
-        if "name" in o:
-            str_name = o["name"]
+        if "name" in obj:
+            str_name = obj["name"]
 
         # create the new part
         part = {
             "id": 0,
-            "part_id": o["id"].split("_")[0],
-            "part_label": o["props"][0]["value"] if "props" in o else None,
-            "name": o["id"],
+            "part_id": obj["id"].split("_")[0],
+            "part_label": obj["props"][0]["value"] if "props" in obj else None,
+            "name": obj["id"],
             "objectiveType": str_name if "objective" not in str_name else "objective",
             "description": str_prose,
             "parentControl": ctrl["id"],
             "parentObjective": parent_id,
         }
-        if "obj" in o["id"] or "item" in o["name"] or "overview" in o["name"]:
+        if "obj" in obj["id"] or "item" in obj["name"] or "overview" in obj["name"]:
             parts.append(part)
-            str_obj += "<li>{{" + o["id"] + "}}"
-            if "prose" in o:
+            str_obj += "<li>{{" + obj["id"] + "}}"
+            if "prose" in obj:
                 str_obj += " - " + str_prose
             str_obj += "</li>"
-            if "parts" in o:
-                str_obj += process_objectives(o["parts"], parts, ctrl, o["id"])
+            if "parts" in obj:
+                str_obj += process_objectives(obj["parts"], parts, ctrl, obj["id"])
     str_obj += "</ul>"
     return str_obj
 
 
 def process_control(
     ctrl, resources, str_family, parameters, parts, assessments
 ) -> dict:
@@ -1597,40 +1622,40 @@
     :param assessments: Assessments belonging to the control
     :return: Dictionary of new control
     :rtype: dict
     """
     # see if parameters exist
     if "params" in ctrl:
         # loop through each parameter
-        for p in ctrl["params"]:
+        for param in ctrl["params"]:
             # create a new parameter object
             p_new = {
-                "name": p["id"],
+                "name": param["id"],
                 "value": "",
                 "paramType": "",
                 "controlId": ctrl["id"],
             }
             # process basic label
-            if "label" in p:
+            if "label" in param:
                 p_new["paramType"] = "text"
-                p_new["value"] = p["label"]
+                p_new["value"] = param["label"]
             else:
                 # initialize
                 str_params = "Select ("
                 # process select types
-                if "select" in p:
-                    select = p["select"]
+                if "select" in param:
+                    select = param["select"]
                     if "how-many" in select:
                         str_params += select["how-many"]
                         p_new["paramType"] = "how-many"
                     if "choice" in select:
                         p_new["paramType"] = "choice"
                         str_params += "select) - "
-                        for z in select["choice"]:
-                            str_params += z + ", "
+                        for cho in select["choice"]:
+                            str_params += cho + ", "
                     p_new["value"] = str_params
             # add to the array
             parameters.append(p_new)
 
     # get enhancements
     str_enhance = ""
     if "controls" in ctrl:
@@ -1704,41 +1729,41 @@
         # initialize
         str_parts = ""
         str_guidance = ""
         str_assessment = ""
 
         # create text field for human display
         str_parts += "<ul>"
-        for p in ctrl["parts"]:
-            if (("id" in p) and (p["name"].startswith("assessment") is False)) or (
-                ("id" in p)
-                and p["name"]
+        for dat in ctrl["parts"]:
+            if (("id" in dat) and (dat["name"].startswith("assessment") is False)) or (
+                ("id" in dat)
+                and dat["name"]
                 in [
                     "item",
                     "assessment-objective",
                     "statement",
                     "objective",
                     "overview",
                 ]
             ):
                 # check prose
                 str_prose = ""
-                if "prose" in p:
-                    str_prose = p["prose"]
+                if "prose" in dat:
+                    str_prose = dat["prose"]
 
                 # check name
                 str_name = ""
-                if "name" in p:
-                    str_name = p["name"]
+                if "name" in dat:
+                    str_name = dat["name"]
 
                 # create the new part
                 part = {
                     "id": 0,
-                    "name": p["id"],
-                    "part_id": p["id"].split("_")[0],
+                    "name": dat["id"],
+                    "part_id": dat["id"].split("_")[0],
                     "objectiveType": str_name,
                     "description": str_prose
                     if str_prose
                     else "Determine if the organization:",
                     "parentControl": ctrl["id"],
                     "parentObjective": "",
                 }
@@ -1748,39 +1773,41 @@
                     str_name
                     in ["assessment-objective", "statement", "objective", "overview"]
                     # or p["name"] == "statement"
                     # or p["name"] == "item"
                 ):
                     parts.append(part)
                     try:
-                        str_parts += "<li>{{" + p["id"] + "}} - " + str_prose + "</li>"
+                        str_parts += (
+                            "<li>{{" + dat["id"] + "}} - " + str_prose + "</li>"
+                        )
                     except Exception:
-                        logger.error("Unable to parse part - %s.", p["id"])
-                    if "parts" in p:
+                        logger.error("Unable to parse part - %s.", dat["id"])
+                    if "parts" in dat:
                         str_parts += process_objectives(
-                            p["parts"], parts, ctrl, p["id"]
+                            dat["parts"], parts, ctrl, dat["id"]
                         )
                 # process guidance
-                if p["name"] in ["guidance", "overview"]:
+                if dat["name"] in ["guidance", "overview"]:
                     str_guidance = "<ul><li>Guidance</li>"
-                    if "prose" in p:
+                    if "prose" in dat:
                         str_guidance += "<ul>"
-                        str_guidance += "<li>" + p["prose"] + "</li>"
+                        str_guidance += "<li>" + dat["prose"] + "</li>"
                         str_guidance += "</ul>"
-                    if "links" in p:
+                    if "links" in dat:
                         str_guidance += "<ul>"
-                        for lkp in p["links"]:
+                        for lkp in dat["links"]:
                             str_guidance += (
                                 "<li>" + lkp["href"] + ", " + lkp["rel"] + "</li>"
                             )
                         str_guidance += "</ul>"
                     str_guidance += "</ul>"
             else:
                 # process assessments
-                process_assessments(p, ctrl, assessments)
+                process_assessments(dat, ctrl, assessments)
 
         str_parts += "</ul>"
     else:
         # no parts - set default values
         str_parts = ""
         str_guidance = ""
         str_assessment = ""
@@ -1790,102 +1817,87 @@
         "parts": str_parts,
         "guidance": str_guidance,
         "assessments": str_assessment,
     }
     return part_info
 
 
-def process_assessments(p, ctrl, assessments) -> None:
+def process_assessments(dat, ctrl, assessments) -> None:
     """
     Process assessment data
     :param p: Data to process
     :param ctrl: Controls
     :param assessments: Assessments
     :return: None
     :rtype: None
     """
     # process assessments
-    if p["name"].startswith("assessment") is True:
+    if dat["name"].startswith("assessment") is True:
         # see if a lowe level objective that has prose
-        if "prose" in p:
+        if "prose" in dat:
             # create new assessment objective
             ast = {
                 "id": 0,
-                "name": p["id"],
-                "testType": p["name"],
-                "description": p["prose"],
+                "name": dat["id"],
+                "testType": dat["name"],
+                "description": dat["prose"],
                 "parentControl": ctrl["id"],
             }
 
             # see if it has any child tests
-            if "parts" in p:
-                if len(p["parts"]) > 0:
-                    for item in p["parts"]:
+            if "parts" in dat:
+                if len(dat["parts"]) > 0:
+                    for item in dat["parts"]:
                         process_assessments(item, ctrl, assessments)
         else:
             # check the id
             str_part_id = ""
-            if "id" in p:
-                str_part_id = p["id"]
+            if "id" in dat:
+                str_part_id = dat["id"]
             else:
                 str_part_id = str(uuid.uuid4())
 
             # handle methods
             ast = {
                 "id": 0,
                 "name": str_part_id,
                 "testType": "",
                 "description": "",
                 "parentControl": ctrl["id"],
             }
-            if "props" in p:
-                if len(p["props"]) > 0:
-                    if "value" in p["props"][0]:
-                        ast["testType"] = p["props"][0]["value"]
-            if "parts" in p:
-                if len(p["parts"]) > 0:
-                    if "prose" in p["parts"][0]:
-                        ast["description"] = p["parts"][0]["prose"]
+            if "props" in dat:
+                if len(dat["props"]) > 0:
+                    if "value" in dat["props"][0]:
+                        ast["testType"] = dat["props"][0]["value"]
+            if "parts" in dat:
+                if len(dat["parts"]) > 0:
+                    if "prose" in dat["parts"][0]:
+                        ast["description"] = dat["parts"][0]["prose"]
 
         # add test of the array
         if ast["description"] != "":
             assessments.append(ast)
 
 
 def oscal_version(metadata: dict) -> str:
     """
     Determine the oscal base version
     :param metadata: The metadata from OSCAL
     :raises ValueError: CLI support statement
     :return: The schema version
     :rtype: str
     """
-    schema = metadata["oscal-version"]
-    if schema not in ["1.0.0", "1.0.2", "1.0.4"]:
+    oscal_schema = metadata["oscal-version"]
+    if oscal_schema not in ["1.0.0", "1.0.2", "1.0.4"]:
         raise ValueError(
-            f"The RegScale CLI does not support OSCAL version {schema}, only 1.0.0, 1.0.2 and 1.0.4 are supported."
-        )
-    logger.info("Oscal Version: %s", schema)
-    return schema
-
-
-def check_oscal() -> None:
-    """
-    Check if oscal dependencies are installed
-    :raises: General error if dependencies aren't found
-    :return: None
-    :rtype: None
-    """
-    if (
-        "unable to access jarfile".lower() in app.get_java()
-        or not oscal_cli_path.exists()
-    ):
-        error_and_exit(
-            "OSCAL CLI library not found, please check init.yaml and add the full cli path.  e.g. /opt/oscal-cli/bin/oscal-cli"
+            f"The RegScale CLI does not support OSCAL version {oscal_schema}, only 1.0.0, 1.0.2 \
+                and 1.0.4 are supported."
         )
+    logger.info("Oscal Version: %s", oscal_schema)
+    return oscal_schema
 
 
 def append_controls(
     oscal_controls, ctrl, resources, str_family, parameters, parts, assessments
 ) -> list[dict]:
     """
     Process and append controls to list
```

## app/utils/app_utils.py

```diff
@@ -13,14 +13,15 @@
 import sys
 from collections import abc
 from datetime import datetime
 from pathlib import Path
 from shutil import copytree, rmtree
 from site import getusersitepackages
 from tempfile import gettempdir
+from urllib.parse import urlparse
 
 import pandas as pd
 import requests
 import xmltodict
 from dateutil import relativedelta
 from rich.progress import (
     BarColumn,
@@ -87,15 +88,16 @@
             mac_address.lower(),
         )
     )
 
 
 def reformat_str_date(date_str: str, dt_format: str = "%m/%d/%Y") -> str:
     """
-    Function to convert a string into a datetime object and reformat it to dt_format, default format is MM/DD/YYYY
+    Function to convert a string into a datetime object and reformat it to dt_format, default \
+        format is MM/DD/YYYY
     :param str date_str: date as a string
     :param str dt_format: datetime string format, defaults to "%m/%d/%Y"
     :return: string with the provided date format
     :rtype: str
     """
     # replace the T with a space and create list of result
     date_str = date_str.replace("T", " ").split(" ")
@@ -374,26 +376,43 @@
     :param str error_desc: Description of the error encountered
     :return: None
     """
     logger.error(error_desc)
     sys.exit(1)
 
 
-def download_file(url: str, download_path: str = gettempdir()) -> Path:
+def check_url(url: str) -> bool:
+    """
+    Function to check if the provided url is valid
+    :param str url: URL to check
+    :return: True if URL is valid, False if not
+    :rtype: bool
+    """
+    try:
+        result = urlparse(url)
+        return all([result.scheme, result.netloc])
+    except ValueError:
+        return False
+
+
+def download_file(
+    url: str, download_path: str = gettempdir(), verify: bool = True
+) -> Path:
     """
     Download file from the provided url and save it to the provided download_path
     :param str url: URL location of the file to download
     :param Path download_path: Path to download the file to
+    :param bool verify: SSL verification for requests, defaults to True
     :return: Path to the downloaded file
     :rtype: Path
     """
     path = Path(download_path)
     local_filename = url.split("/")[-1]
     # NOTE the stream=True parameter below
-    with requests.get(url, stream=True, timeout=10) as response:
+    with requests.get(url, stream=True, timeout=10, verify=verify) as response:
         response.raise_for_status()
         with open(path / local_filename, "wb+") as file:
             for chunk in response.iter_content(chunk_size=8192):
                 # If you have chunk encoded response uncomment if
                 # and set chunk_size parameter to None.
                 # if chunk:
                 file.write(chunk)
@@ -613,15 +632,16 @@
     if isinstance(obj, list):
         for key in obj:
             htmls.append(format_data_to_html(key, indent + 1))
 
     if isinstance(obj, dict):
         for key, val in obj.items():
             htmls.append(
-                f"<span style='font-style: italic; color: #888'>{key}</span>: {format_data_to_html(val, indent + 1)}"
+                f"<span style='font-style: italic; color: #888'>{key}</span>: \
+                {format_data_to_html(val, indent + 1)}"
             )
 
     if htmls:
         return f'<div style="margin-left: {indent}em">{",<br>".join(htmls)}</div>'
 
     return str(obj)
```

## tests/test_evidence.py

```diff
@@ -23,33 +23,28 @@
 api = Api(app)
 config = {}
 try:
     # load the config from YAML
     config = app.load_config()
 except FileNotFoundError:
     logger.error("ERROR: No init.yaml file or permission error when opening file.")
-EVIDENCE_FOLDER = ""
 
 
 class TestEvidence:
     """
     Tests for evidence.py
     """
 
-    def test_init(self):
-        """
-        Figure out current working directory and set EVIDENCE_FOLDER accordingly
-        """
-        global EVIDENCE_FOLDER
-        cur_dir = Path(os.getcwd())
-        if cur_dir.stem.lower() == "tests":
-            EVIDENCE_FOLDER = f'.{app.config["evidenceFolder"]}'
-        else:
-            EVIDENCE_FOLDER = app.config["evidenceFolder"]
-        check_file_path(EVIDENCE_FOLDER)
+    evidence_folder = None
+    cur_dir = Path(os.getcwd())
+    if cur_dir.stem.lower() == "tests":
+        evidence_folder = f'.{app.config["evidenceFolder"]}'
+    else:
+        evidence_folder = app.config["evidenceFolder"]
+    check_file_path(evidence_folder)
 
     def test_remove(self):
         """
         Test item removal
         """
         # create test list
         test_list = [".test1.csv", ".test2.docx", ".test3.pdf"]
@@ -100,47 +95,47 @@
 
     def test_find_signatures(self):
         """
         Determine if the file is digitally signed
         """
         file_name = "test.pdf"
         # check if the file is a pdf document
-        if file_name.endswith(".pdf"):
+        if file_name.endswith(".pdf") and os.path.isfile(file_name):
             try:
                 # open the document
                 doc = fitz.open(file_name)
                 # if document is not found
             except fitz.fitz.FileNotFoundError:
                 # set number to 0
                 number = 0
             else:
                 # determine if document is digitally signed
                 number = doc.get_sigflags()
         # if the file is a docx document
-        if not file_name.endswith(".pdf"):
+        else:
             # set sig flag equal to 0
             number = 0
         # assert number is an integer
         assert isinstance(number, int)
         # assert number one of the values below
         assert number in [-1, 0, 1, 3]
 
     def test_set_directory_variables(self):
         """
         Test setting evidence folder directory variables
         """
         dir_name = [
             filename
-            for filename in os.listdir(EVIDENCE_FOLDER)
-            if os.path.isdir(os.path.join(EVIDENCE_FOLDER, filename))
+            for filename in os.listdir(self.evidence_folder)
+            if os.path.isdir(os.path.join(self.evidence_folder, filename))
         ][0]
         # pick up subdirectory under the evidence folder
-        new_cwd = EVIDENCE_FOLDER + os.sep + dir_name
-        assert isinstance(EVIDENCE_FOLDER, str)
-        assert len(EVIDENCE_FOLDER) > 0
+        new_cwd = self.evidence_folder + os.sep + dir_name
+        assert isinstance(self.evidence_folder, str)
+        assert len(self.evidence_folder) > 0
         assert isinstance(dir_name, str)
         assert len(dir_name) > 0
         assert isinstance(new_cwd, str)
         assert len(new_cwd) > 0
 
     def test_parse_required_docs(self):
         """
@@ -148,15 +143,15 @@
         """
         # create an empty list to hold a list of all document requirements for the assessment
         required_docs = []
         # create an empty list to hold a list of all required documents
         document_list = set()
         # open app//evidence//config.json file and read contents
         with open(
-            f"{EVIDENCE_FOLDER}{os.sep}config.json", "r", encoding="utf-8"
+            f"{self.evidence_folder}{os.sep}config.json", "r", encoding="utf-8"
         ) as json_file:
             # load json object into a readable dictionary
             rules = json.load(json_file)
             # loop through required document dicts
             for i in range(len(rules["required-documents"])):
                 # add to a list of dictionaries for parsing
                 required_docs.append(
@@ -191,19 +186,19 @@
     def test_get_doc_timestamps(self):
         """
         Test geting document timestamps
         """
         # extract directory name from evidence folder
         dir_name = [
             filename
-            for filename in os.listdir(EVIDENCE_FOLDER)
-            if os.path.isdir(os.path.join(EVIDENCE_FOLDER, filename))
+            for filename in os.listdir(self.evidence_folder)
+            if os.path.isdir(os.path.join(self.evidence_folder, filename))
         ][0]
         # set evidence folder directory location
-        local_evidence_folder = f"{EVIDENCE_FOLDER}{os.sep}{dir_name}"
+        local_evidence_folder = f"{self.evidence_folder}{os.sep}{dir_name}"
         # create empty list to hold file modified times
         modified_times = []
         # get list of folders in parent folder
         folders_list = os.listdir(local_evidence_folder)
 
         # remove any child folders that start with '.'
         def remove(list_to_review):
@@ -229,15 +224,17 @@
             for filename in newlist:
                 # append the modified time for each file to the list
                 modified_times.append(
                     {
                         "program": folder,
                         "file": filename,
                         "last-modified": os.path.getmtime(
-                            os.path.join(EVIDENCE_FOLDER, dir_name, folder, filename)
+                            os.path.join(
+                                self.evidence_folder, dir_name, folder, filename
+                            )
                         ),
                     }
                 )
 
         # loop through the list of timestamps
         def delta(time):
             """
@@ -278,15 +275,15 @@
         required_text = set()
         # create an empty list to hold all texts in the file
         all_texts = []
         # create an empty list to hold the unique texts in the file
         unique_texts = []
         # open app//evidence//config.json file and read contents
         with open(
-            f"{EVIDENCE_FOLDER}{os.sep}config.json", "r", encoding="utf-8"
+            f"{self.evidence_folder}{os.sep}config.json", "r", encoding="utf-8"
         ) as json_file:
             # load json object into a readable dictionary
             rules = json.load(json_file)
             # create iterator to traverse dictionary
             for i in range(len(rules["rules-engine"])):
                 # pull out required text to look for from config
                 for items in rules["rules-engine"][i]["text-to-find"]:
@@ -311,19 +308,19 @@
     def test_find_required_files_in_folder(self):
         """
         Pull out required files from each directory for parsing
         """
         # create directory name
         dir_name = [
             filename
-            for filename in os.listdir(EVIDENCE_FOLDER)
-            if os.path.isdir(os.path.join(EVIDENCE_FOLDER, filename))
+            for filename in os.listdir(self.evidence_folder)
+            if os.path.isdir(os.path.join(self.evidence_folder, filename))
         ][0]
         # create evidence folder
-        local_evidence_folder = f"{EVIDENCE_FOLDER}{os.sep}{dir_name}"
+        local_evidence_folder = f"{self.evidence_folder}{os.sep}{dir_name}"
         # create empty list to hold list of files in directory
         dir_list = []
         # build a list of all folders to iterate through
         folder_list = os.listdir(local_evidence_folder)
 
         # remove any folders starting with '.' from list
         def remove(list_to_review):
```

## Comparing `RegScale_CLI-4.25.1.dist-info/LICENSE` & `RegScale_CLI-4.26.0.dist-info/LICENSE`

 * *Files identical despite different names*

## Comparing `RegScale_CLI-4.25.1.dist-info/METADATA` & `RegScale_CLI-4.26.0.dist-info/METADATA`

 * *Files 0% similar despite different names*

```diff
@@ -1,10 +1,10 @@
 Metadata-Version: 2.1
 Name: RegScale-CLI
-Version: 4.25.1
+Version: 4.26.0
 Summary: Command Line Interface (CLI) for bulk processing/loading data into RegScale
 Home-page: https://github.com/RegScale/regscale-cli
 Author: Travis Howerton
 Author-email: thowerton@regscale.com
 License: MIT
 Platform: UNKNOWN
 Classifier: Operating System :: OS Independent
```

## Comparing `RegScale_CLI-4.25.1.dist-info/RECORD` & `RegScale_CLI-4.26.0.dist-info/RECORD`

 * *Files 6% similar despite different names*

```diff
@@ -1,48 +1,54 @@
-regscale.py,sha256=-QqDONm-p2MqbrqH6HMYbMt6FS9d7-KZOnZAjiDJHVY,14485
+regscale.py,sha256=ekk6Rc1Ef7Ej2WYA1FkSjAg0g5B4ujPUToCsrZyPkTg,14585
 app/__init__.py,sha256=gbUXYeGrcrqcOc70BjFtxHtBnwSqor-SlWYgZjypEOA,47
-app/_version.py,sha256=7rL0JrwSTgN8NJAeI_9_-UTloDwa79yBvaJPuK8a1sM,73
+app/_version.py,sha256=_txzyLyqxxtdB_ovV5GTeV6lIF8l4gZKy5jKZrq_q6w,73
 app/api.py,sha256=yezBIkGP5y-CKO-7b0f00T_q5l8-Kyaw1kXHVQ8Ee_U,14497
 app/application.py,sha256=Qj9VzhImSvAoBCJQI5SMT-vVn_4Rxfsk4POQNDsh_j0,13397
 app/logz.py,sha256=p7ADwGAkz75cey8CS6XdsS--jp9WnFRZANm02ZhtFAk,1287
 app/commercial/__init__.py,sha256=47DEQpj8HBSa-_TImW-5JCeuQeRkm5NMpJWZG3hSuFU,0
 app/commercial/ad.py,sha256=CTt7F5mNznZGHJkgEK5zSCozmP858gqJyVti1ogkh98,16359
 app/commercial/defender.py,sha256=Crt8VKp3BRZvqMz_irfZ9cOAPEMs5kXkKYmchG-q1zs,45796
 app/commercial/jira.py,sha256=bqdbbp79JN6GBL5mfAg-sHEfwWyzTlobLYPw7bCUhko,8007
 app/commercial/okta.py,sha256=P0iJ59akglkEU0EhvSILnVT799VTMkDtlQi1Tl_u3TE,27240
 app/commercial/qualys.py,sha256=IPhxBjOp8FvI4ebM4Xwdpl5ef7UJ8y2eefgzQ-zE_kY,29595
 app/commercial/servicenow.py,sha256=nzQVHzdw02UpJDtxI850pCwipPUCGA19ijg8bi_WJUE,11689
-app/commercial/stig.py,sha256=I9RjHqgpNiiXtkSwTqAcz7Cxw4nMiIzJ3FxgSLVJDaM,62786
+app/commercial/stig.py,sha256=rgehL-P0ASQkoVuz6U05NQvFzpKmgGYmdz5ZNXaoyVU,62800
 app/commercial/tenable.py,sha256=NeoG2BCagPD5s8A78xf8kED6Wmu076AmmrPW8bVB-Xc,22634
 app/commercial/wiz.py,sha256=P5wInsQ7HvyltQW_KccT113IkmGCRK9e_TujJi5FALk,56457
 app/internal/__init__.py,sha256=47DEQpj8HBSa-_TImW-5JCeuQeRkm5NMpJWZG3hSuFU,0
 app/internal/admin_actions.py,sha256=Ry1x_dmM2odXmVZpBB3R3Wgs8bj3qCC4acsTA-Pel8A,26998
 app/internal/assessments_editor.py,sha256=boaP7PTNFO831WLuDP3o5cRXT6P7H6g9Wt3WJWYDE_I,31042
+app/internal/catalog.py,sha256=pHBL9UGyB9f0c2X7kh89dKiYMJ0FC4Pq3z6MkpKd5ZQ,910
 app/internal/comparison.py,sha256=F4VcAUpxzBctouUVHq_9cC_VC46qkNxtDZs93Lc45tw,16114
 app/internal/control_editor.py,sha256=eTb8p_JJS7g64zj9wqMXnHZh6OynfAHYzzox7P4N6nY,15277
 app/internal/encrypt.py,sha256=UTs3mwTxackgYhr357npx6rqXz7X2gMJ9mTaQ5hBsKw,5912
 app/internal/evidence.py,sha256=94ew7Vg-FeYm4q7RUha8S4vk3Ky-Hnpmc-qVIsrLwLQ,40122
 app/internal/healthcheck.py,sha256=K6NNvgKucBl1Kg5GO-XiENfISQXk331t-KzhIHSPlPE,2325
-app/internal/login.py,sha256=vk5VwrCQU18Al-nayPQaNin5lbXEx4jiQFmyizecQ08,6397
-app/internal/migrations.py,sha256=4s_QoBJPDrHI9jMvYMZVady4GbEbGMlEK41hrhsA-iw,9419
+app/internal/login.py,sha256=tlb92GPKFAvt-ynahwvBfNz1QQ9rTLFFf4qG2uiNGRU,6471
+app/internal/migrations.py,sha256=KzVJxzX7DmEa7XNQQyWpt9NnYFP5TE7r0Flsnc-_edk,8731
 app/public/__init__.py,sha256=47DEQpj8HBSa-_TImW-5JCeuQeRkm5NMpJWZG3hSuFU,0
 app/public/cisa.py,sha256=ON5phwcGZYqOFk-JlkVvca_K5E9H0C6EXyN1rwjaC74,18601
 app/public/emass.py,sha256=Jm5B_1Sw3g7vEAVGnioWK4UclCALjVLWR5TTP2F3Ovw,11186
 app/public/fedramp.py,sha256=n_B4AqYk4vXHt3TIf8zgVJShEYb4gINxFwUSRFeOpSk,63918
 app/public/nist_catalog.py,sha256=MnGyJ1hNvqnfrBjtUkTIBYlV0oeQsQYpp1hZ7Y7IPZE,7946
-app/public/oscal.py,sha256=1fn5USWnq3LN-E5w0WpIlbOvFEyVY73PuYpTs5GBF9k,71111
+app/public/oscal.py,sha256=Qkrioo-Y_-E9dFLsBSegCmQq6O_xT0qoR03kQ5Hr-Bs,72156
 app/public/otx.py,sha256=Q_4BnAKpij6lGC_I71uT5ruarP4laWF20Z4zGBXo8-U,6081
 app/utils/__init__.py,sha256=47DEQpj8HBSa-_TImW-5JCeuQeRkm5NMpJWZG3hSuFU,0
-app/utils/app_utils.py,sha256=ZfsEN9GoJhlQbIzlpFw9x9333CO0lX7uI5BG25t6k10,21592
+app/utils/app_utils.py,sha256=oFSNwFfELZDWzS3bnENaDn7gUoC2d4FaySejxqfCxIc,22103
 app/utils/regscale_utils.py,sha256=zGj9J7oxfgbONkktSNh2QO0ZFcS90AeqdN6xe4qCW0Q,10409
 app/utils/threadhandler.py,sha256=XcKXSYSQeJla1Vd5RPryt4f5yW_-P4kXIEmw_JuF3Z8,1591
+app/utils/catalog_utils/__init__.py,sha256=47DEQpj8HBSa-_TImW-5JCeuQeRkm5NMpJWZG3hSuFU,0
+app/utils/catalog_utils/compare_catalog.py,sha256=zgQJ6ngKqbUJxKD68PYWRha5CMqLlCqZ8SETAqKkJKE,7343
+app/utils/catalog_utils/diagnostic_catalog.py,sha256=MuT7w0mEnCynEs00cPrTshMDjR2nhVbWNnoYZP1AWVk,4491
+app/utils/catalog_utils/export_catalog.py,sha256=L-EKmOL-1nUY4XcXaL6VJEjB-gQzuQfX-38Rch2NckE,2653
 exceptions/__init__.py,sha256=47DEQpj8HBSa-_TImW-5JCeuQeRkm5NMpJWZG3hSuFU,0
 exceptions/license_exception.py,sha256=5lDYW1uGf7dHFKBkhzYD3FlNnI6W4BICXi24OJyOs_w,195
 models/__init__.py,sha256=GLPPGTA5Lczf4OWUnudaFTG4OV2Hqt6IjZymYO6HL04,79
 models/app_models/__init__.py,sha256=47DEQpj8HBSa-_TImW-5JCeuQeRkm5NMpJWZG3hSuFU,0
+models/app_models/catalog_compare.py,sha256=8inbqggl-_iAm4PopvI88hgeV_V5FgN_kxAwETHCWK8,6556
 models/app_models/click.py,sha256=o2eu7-AZFBQtdCb1USHYmq7DKYMyh01Tx3q-oYpDhBA,3997
 models/app_models/control_editor.py,sha256=F0Ky7As96UvUBu-W_WfDoObJBfs94t-w3zcFdTslT-Q,13863
 models/app_models/pipeline.py,sha256=13BuBreZESL7SP7ajCFXy8bgaEchTnyGlaRtUMBl0bM,889
 models/integration_models/__init__.py,sha256=47DEQpj8HBSa-_TImW-5JCeuQeRkm5NMpJWZG3hSuFU,0
 models/integration_models/azure_alerts.py,sha256=I_jcjaLMsQpgabkdtVbalGZ23GeAH8aQCmH9iXlKFhw,7858
 models/integration_models/recommendations.py,sha256=T_zyM1G4xFsZgApVvz8CGUMYuZLzfXLgoAcXsz1xUKE,872
 models/integration_models/tenable.py,sha256=ssbs0p5VNqWBvQSwDqNchVHQOxhzblnd1F_Zjf9NAJ8,8319
@@ -58,35 +64,35 @@
 models/regscale_models/implementation_option.py,sha256=msHRYWn2hwQ1VNVx4v06tHhWwc57cx-9lYfRgaJl8Tk,2981
 models/regscale_models/interconnects.py,sha256=Amv3CSrurRwDOaLBiqioLNWlmhAqcq6Pbsm8ekv1Yk8,2850
 models/regscale_models/issue.py,sha256=veNXQW_hkx4_3_opiSaIE6_kuUoVR92WbB6SBCCjc9U,4175
 models/regscale_models/modules.py,sha256=Gdjv4CAOrUNeOHI7NKJ84MlpVX8hwOd-bhujJQ1pgEI,4878
 models/regscale_models/objective.py,sha256=-qwNIEyAN2st1MzANxgQDysSiqspRGRwjdW6wlhtL9I,232
 models/regscale_models/ports_protocols.py,sha256=qupVuXfeNXxMBW8EviLDgtBJc3id9YvxfcJibHpjojo,2353
 models/regscale_models/requirements.py,sha256=msCh2iZD9YujpJO5yakfRRKebd1KE_wmbQIFe-iJbHQ,2415
+models/regscale_models/security_control.py,sha256=Eeqwqc4xsbKMqZvn8xgFonXinURSKRgswIymzG5WkCk,5949
 models/regscale_models/securityplans.py,sha256=93qb92V2W_hOf2hOhLLw7J087LngCgzo91Zzg9gQ1S8,5886
 models/regscale_models/stig.py,sha256=DUS5SUpaI-0bPuP2b_mXsYWordxHCnAqYGrdcrANdVY,25813
 models/regscale_models/threat.py,sha256=eRO_tPT4pOTJvMGYAQ9bU7QHs2HnFBLiIWY8Jyu1pQk,1425
 models/regscale_models/user.py,sha256=mm-1ajFmCFzDeHDAydcANdQMIBehfCRhjWCCoMKZXFY,2242
 tests/__init__.py,sha256=47DEQpj8HBSa-_TImW-5JCeuQeRkm5NMpJWZG3hSuFU,0
 tests/conftest.py,sha256=7Ba9a9CreEyUJJbxXolUQnHuOt3lrI-lnsOch8N390I,1818
-tests/mock_api_service.py,sha256=52ih6mY0II1e7z-e3JxK0sQF6WTwa2MnObgL3WcoTxs,368
 tests/test_app_utils.py,sha256=CP8ZQVCVdTRKy97QICKc4_4cHWQYQPd9KZw4vivAAWY,1334
 tests/test_assessments_editor.py,sha256=JK-ap6JVnF3SABLLkKvy-OPRi8sPkcjwhbz0yjxns3Y,1930
 tests/test_cisa.py,sha256=2tpRm-hB-bYl01lif8UNwhJETFuRm_R0UQBC3SCfcwk,1687
 tests/test_control_editor.py,sha256=kad6vmaeeNmkUWp1uETRsGPDsF1TeMFjxfln2lZJPlY,1747
 tests/test_dependabot.py,sha256=tTQZnikbzxW7v5LAMucZbDHQuwnM3UttZ8KLXl3KMts,1425
 tests/test_emass.py,sha256=2-w7c77ZDzkLKiGBjmUejdjrddLounVTbr7iPqlKhVc,2827
-tests/test_evidence.py,sha256=q4KwOyiJ3NLsoedifYa3Tcg4_cggKkozftwVdz3IbxU,14137
+tests/test_evidence.py,sha256=5bJaSSkCqb2Wg8ItR6i4T6ogwQjWy-yTt4s9TqH8axg,14091
 tests/test_login.py,sha256=u_LPs_H_g8ubARUKHdvPZl3flt2SHEjwFjmWuPnZYLU,1186
 tests/test_npm_audit.py,sha256=XdF0FM8telb2nzhVUc0gT4Zu7Srq3GiJheJFKSWICnk,1416
 tests/test_oscal.py,sha256=Lcm1f0hTr6Iwu5LGU-sKSC55bBsQVm2WX_zeiPBAdbA,9248
 tests/test_snow.py,sha256=tPdi-gWE_kmiv_dZV50T3vV6OB9vgbHr_jBGUi4hCaw,866
 tests/test_sonarcloud.py,sha256=Um1WqpIHRMWyPN7GF-pj263vt2YWcS2Sk7px9f1Ej-o,1245
 tests/test_stig.py,sha256=BNIPmY3ydqeV6nIkCZDCPV7kKYivS1gl4LNqAQ2CnNw,1258
 tests/test_tenable.py,sha256=Ik7GTnKzzXwykQH-VFsBeJPCtjqKx8LA91RCmiHNFpo,1666
 tests/test_update_regscale_config.py,sha256=GoU2ZKh7_2RYFj742_oFz8WuQx4NWL6nx2rzs4TSr40,1254
-RegScale_CLI-4.25.1.dist-info/LICENSE,sha256=ytNhYQ9Rmhj_m-EX2pPq9Ld6tH5wrqqDYg-fCf46WDU,1076
-RegScale_CLI-4.25.1.dist-info/METADATA,sha256=n_ZoZpGBZnDqaLPafGr79mixLVqQ0KoFwFju0QCh-pY,6330
-RegScale_CLI-4.25.1.dist-info/WHEEL,sha256=G16H4A3IeoQmnOrYV4ueZGKSjhipXx8zc8nu9FGlvMA,92
-RegScale_CLI-4.25.1.dist-info/entry_points.txt,sha256=7K_c4hLFOPUjCZbuY5gat-C4fVj0-4aqxbwBon21Nnc,43
-RegScale_CLI-4.25.1.dist-info/top_level.txt,sha256=6YXQTQW1Iq23W68s8qehQk2zgUCqyCCr6miOwR-RiPM,37
-RegScale_CLI-4.25.1.dist-info/RECORD,,
+RegScale_CLI-4.26.0.dist-info/LICENSE,sha256=ytNhYQ9Rmhj_m-EX2pPq9Ld6tH5wrqqDYg-fCf46WDU,1076
+RegScale_CLI-4.26.0.dist-info/METADATA,sha256=rgLxX_ZCLIip2vkbDr9LNbwN2XpnWQn7z8yuJ58bpMA,6330
+RegScale_CLI-4.26.0.dist-info/WHEEL,sha256=G16H4A3IeoQmnOrYV4ueZGKSjhipXx8zc8nu9FGlvMA,92
+RegScale_CLI-4.26.0.dist-info/entry_points.txt,sha256=7K_c4hLFOPUjCZbuY5gat-C4fVj0-4aqxbwBon21Nnc,43
+RegScale_CLI-4.26.0.dist-info/top_level.txt,sha256=6YXQTQW1Iq23W68s8qehQk2zgUCqyCCr6miOwR-RiPM,37
+RegScale_CLI-4.26.0.dist-info/RECORD,,
```

