# Comparing `tmp/lightningchart-0.3.6-py3-none-any.whl.zip` & `tmp/lightningchart-0.3.7-py3-none-any.whl.zip`

## zipinfo {}

```diff
@@ -1,17 +1,17 @@
-Zip file size: 5136200 bytes, number of entries: 34
--rw-rw-rw-  2.0 fat     1542 b- defN 23-Apr-16 14:55 lightningchart/__init__.py
--rw-rw-rw-  2.0 fat    73241 b- defN 23-Apr-03 05:03 lightningchart/charts.py
+Zip file size: 5136443 bytes, number of entries: 33
+-rw-rw-rw-  2.0 fat     1671 b- defN 23-Apr-19 09:05 lightningchart/__init__.py
+-rw-rw-rw-  2.0 fat    79560 b- defN 23-Apr-19 09:25 lightningchart/charts.py
 -rw-rw-rw-  2.0 fat       54 b- defN 23-Mar-31 07:40 lightningchart/globals.py
--rw-rw-rw-  2.0 fat     8507 b- defN 23-Apr-18 04:22 lightningchart/instance.py
+-rw-rw-rw-  2.0 fat     8593 b- defN 23-Apr-19 08:17 lightningchart/instance.py
 -rw-rw-rw-  2.0 fat    49926 b- defN 23-Apr-17 05:41 lightningchart/series.py
 -rw-rw-rw-  2.0 fat     5369 b- defN 23-Apr-16 14:58 lightningchart/server.py
 -rw-rw-rw-  2.0 fat    10116 b- defN 23-Mar-21 07:25 lightningchart/ui_element.py
 -rw-rw-rw-  2.0 fat    12577 b- defN 23-Apr-16 14:59 lightningchart/utils.py
--rw-rw-rw-  2.0 fat    34050 b- defN 23-Apr-18 04:28 lightningchart/static/functions.js
+-rw-rw-rw-  2.0 fat    35397 b- defN 23-Apr-19 08:21 lightningchart/static/functions.js
 -rw-rw-rw-  2.0 fat    24307 b- defN 23-Mar-06 12:35 lightningchart/static/lcjs-themes.iife.js
 -rw-rw-rw-  2.0 fat  1325425 b- defN 23-Feb-14 11:57 lightningchart/static/lcjs.iife.js
 -rw-rw-rw-  2.0 fat     7397 b- defN 23-Mar-27 10:29 lightningchart/static/msgpack.min.js
 -rw-rw-rw-  2.0 fat    45806 b- defN 23-Feb-21 10:46 lightningchart/static/socket.io.min.js
 -rw-rw-rw-  2.0 fat     2129 b- defN 23-Apr-16 14:54 lightningchart/static/websocket.js
 -rw-rw-rw-  2.0 fat   470264 b- defN 23-Feb-07 08:08 lightningchart/static/resources/maps/countries_africa.json
 -rw-rw-rw-  2.0 fat  1852822 b- defN 23-Feb-07 08:08 lightningchart/static/resources/maps/countries_asia.json
@@ -23,14 +23,13 @@
 -rw-rw-rw-  2.0 fat  1036787 b- defN 23-Feb-07 08:08 lightningchart/static/resources/maps/territoriesProvinces_canada.json
 -rw-rw-rw-  2.0 fat   237008 b- defN 23-Feb-07 08:08 lightningchart/static/resources/maps/territories_australia.json
 -rw-rw-rw-  2.0 fat     4809 b- defN 23-Feb-07 08:08 lightningchart/static/resources/osm/osm-sprite-transparent.png
 -rw-rw-rw-  2.0 fat     1246 b- defN 23-Feb-07 08:08 lightningchart/static/resources/themes/LICENSE
 -rw-rw-rw-  2.0 fat   442543 b- defN 23-Feb-07 08:08 lightningchart/static/resources/themes/cyberSpace.jpg
 -rw-rw-rw-  2.0 fat   495667 b- defN 23-Feb-07 08:08 lightningchart/static/resources/themes/turquoiseHexagon.jpg
 -rw-rw-rw-  2.0 fat     1347 b- defN 23-Apr-16 14:54 lightningchart/templates/index.html
--rw-rw-rw-  2.0 fat     1094 b- defN 23-Apr-18 04:35 lightningchart-0.3.6.dist-info/LICENSE
--rw-rw-rw-  2.0 fat      551 b- defN 23-Apr-18 04:35 lightningchart-0.3.6.dist-info/METADATA
--rw-rw-rw-  2.0 fat       92 b- defN 23-Apr-18 04:35 lightningchart-0.3.6.dist-info/WHEEL
--rw-rw-rw-  2.0 fat       62 b- defN 23-Apr-18 04:35 lightningchart-0.3.6.dist-info/entry_points.txt
--rw-rw-rw-  2.0 fat       15 b- defN 23-Apr-18 04:35 lightningchart-0.3.6.dist-info/top_level.txt
-?rw-rw-r--  2.0 fat     3368 b- defN 23-Apr-18 04:35 lightningchart-0.3.6.dist-info/RECORD
-34 files, 14951171 bytes uncompressed, 5130686 bytes compressed:  65.7%
+-rw-rw-rw-  2.0 fat      529 b- defN 23-Apr-19 09:47 lightningchart-0.3.7.dist-info/METADATA
+-rw-rw-rw-  2.0 fat       92 b- defN 23-Apr-19 09:47 lightningchart-0.3.7.dist-info/WHEEL
+-rw-rw-rw-  2.0 fat       62 b- defN 23-Apr-19 09:47 lightningchart-0.3.7.dist-info/entry_points.txt
+-rw-rw-rw-  2.0 fat       15 b- defN 23-Apr-19 09:47 lightningchart-0.3.7.dist-info/top_level.txt
+?rw-rw-r--  2.0 fat     3273 b- defN 23-Apr-19 09:47 lightningchart-0.3.7.dist-info/RECORD
+33 files, 14957841 bytes uncompressed, 5131081 bytes compressed:  65.7%
```

## zipnote {}

```diff
@@ -78,26 +78,23 @@
 
 Filename: lightningchart/static/resources/themes/turquoiseHexagon.jpg
 Comment: 
 
 Filename: lightningchart/templates/index.html
 Comment: 
 
-Filename: lightningchart-0.3.6.dist-info/LICENSE
+Filename: lightningchart-0.3.7.dist-info/METADATA
 Comment: 
 
-Filename: lightningchart-0.3.6.dist-info/METADATA
+Filename: lightningchart-0.3.7.dist-info/WHEEL
 Comment: 
 
-Filename: lightningchart-0.3.6.dist-info/WHEEL
+Filename: lightningchart-0.3.7.dist-info/entry_points.txt
 Comment: 
 
-Filename: lightningchart-0.3.6.dist-info/entry_points.txt
+Filename: lightningchart-0.3.7.dist-info/top_level.txt
 Comment: 
 
-Filename: lightningchart-0.3.6.dist-info/top_level.txt
-Comment: 
-
-Filename: lightningchart-0.3.6.dist-info/RECORD
+Filename: lightningchart-0.3.7.dist-info/RECORD
 Comment: 
 
 Zip file comment:
```

## lightningchart/__init__.py

```diff
@@ -2,14 +2,16 @@
 import requests
 import socket
 
 from .globals import *
 from .utils import *
 from .server import start_thread
 from .instance import Instance
+from .charts import ChartXY, Chart3D, Dashboard, \
+    PieChart, GaugeChart, PyramidChart, FunnelChart, LineChart, ScatterPlot
 
 
 def init():
     pass
 
 
 def port_is_in_use(port: int) -> bool:
```

## lightningchart/charts.py

```diff
@@ -1,12 +1,13 @@
 from __future__ import annotations
 import datetime
 import uuid
 from builtins import float
 
+import lightningchart
 from .instance import *
 from .series import *
 from .series import Series
 from lightningchart.ui_element import *
 from .ui_element import Axis
 
 
@@ -23,14 +24,22 @@
 
 class Chart:
     def __init__(self, instance: Instance):
         self.id = str(uuid.uuid4().hex)
         self.instance = instance
 
 
+class OpenChart(Chart):
+    def open(self):
+        self.instance.open()
+        return self
+
+    show = open
+
+
 """"""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
 
 
 class GeneralMethods(Chart):
 
     def save_to_file(
             self,
@@ -445,23 +454,34 @@
         self.instance.send_command(self.id, 'setSliceEffect', {'enabled': enabled})
         return self
 
 
 """"""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
 
 
-class ChartXY(GeneralMethods, TitleMethods, ChartWithXYAxis, ChartWithSeries):
+class ChartXY(OpenChart, GeneralMethods, TitleMethods, ChartWithXYAxis, ChartWithSeries):
     """Chart type for visualizing data between two dimensions, X and Y."""
 
     def __init__(
             self,
-            instance: Instance,
-            theme: str = 'darkGold',
+            theme: str = None,
             title: str = None
     ):
+        """Create a XY Chart.
+
+        Args:
+            theme (str): "darkGold" | "light" | "flatDark" | "flatLight" | "lightNature" |
+                "cyberSpace" | "turquoiseHexagon"
+            title (str): A title for the chart.
+
+        Returns:
+            Reference to XY Chart class.
+        """
+
+        instance = lightningchart.Instance()
         ChartWithSeries.__init__(self, instance)
         ChartWithXYAxis.__init__(self)
         self.instance.send_command(self.id, 'chartXY', {'theme': theme})
         if title:
             self.instance.send_command(self.id, 'setTitle', {'title': title})
 
     def __str__(self):
@@ -891,23 +911,132 @@
     def __str__(self):
         return 'XY Chart'
 
 
 """"""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
 
 
-class Chart3D(GeneralMethods, TitleMethods, ChartWithXYAxis, ChartWithSeries):
+class LineChart(ChartXY):
+
+    def __init__(self, x=None, y=None, theme=None, title=None):
+        instance = lightningchart.Instance()
+        ChartWithSeries.__init__(self, instance)
+        ChartWithXYAxis.__init__(self)
+        self.series = str(uuid.uuid4().hex)
+        self.instance.send_command(self.id, 'chartXY', {'theme': theme})
+        if title:
+            self.instance.send_command(self.id, 'setTitle', {'title': title})
+        self.instance.send_command(self.series, 'lineSeries2D', {
+            'chart': self.id,
+            'pattern': None,
+            'xAxis': None,
+            'yAxis': None,
+            'individualLookupValuesEnabled': None
+        })
+        self.instance.send_command(self.series, 'setLineThickness', {'width': 10})
+        if x or y:
+            self.add(x, y)
+
+    def add(self, x=None, y=None):
+        if x and y:
+            self.instance.send_command(self.series, 'addArraysXY', {'arrayX': x, 'arrayY': y})
+        elif y:
+            self.instance.send_command(self.series, 'addArrayY', {'array': y})
+        elif x:
+            self.instance.send_command(self.series, 'addArrayX', {'array': x})
+
+    def add_data(
+            self,
+            data: dict[str, int | float] | list[dict[str, int | float]],
+            clear_previous: bool = False
+    ):
+        """Append a single datapoint or list of datapoints into the chart.
+
+        Args:
+            data (dict[str, int | float] | list[dict[str, int | float]]): List of datapoints or a single datapoint.
+            clear_previous (bool): If true, all previous datapoints are removed from the series.
+
+        Returns:
+            Reference to self.
+        """
+        self.instance.send_data(self.series, data, clear_previous)
+        return self
+
+
+""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
+
+
+class ScatterPlot(ChartXY):
+
+    def __init__(self, x=None, y=None, theme=None, title=None):
+        instance = lightningchart.Instance()
+        ChartWithSeries.__init__(self, instance)
+        ChartWithXYAxis.__init__(self)
+        self.series = str(uuid.uuid4().hex)
+        self.instance.send_command(self.id, 'chartXY', {'theme': theme})
+        if title:
+            self.instance.send_command(self.id, 'setTitle', {'title': title})
+        self.instance.send_command(self.series, 'pointSeries2D', {
+            'chart': self.id,
+            'shape': 1,
+            'xAxis': None,
+            'yAxis': None
+        })
+        self.instance.send_command(self.series, 'setPoint2DSize', {'size': 10})
+        if x or y:
+            self.add(x, y)
+
+    def add(self, x=None, y=None):
+        if x and y:
+            self.instance.send_command(self.series, 'addArraysXY', {'arrayX': x, 'arrayY': y})
+        elif y:
+            self.instance.send_command(self.series, 'addArrayY', {'array': y})
+        elif x:
+            self.instance.send_command(self.series, 'addArrayX', {'array': x})
+
+    def add_data(
+            self,
+            data: dict[str, int | float] | list[dict[str, int | float]],
+            clear_previous: bool = False
+    ):
+        """Append a single datapoint or list of datapoints into the chart.
+
+        Args:
+            data (dict[str, int | float] | list[dict[str, int | float]]): List of datapoints or a single datapoint.
+            clear_previous (bool): If true, all previous datapoints are removed from the series.
+
+        Returns:
+            Reference to self.
+        """
+        self.instance.send_data(self.series, data, clear_previous)
+        return self
+
+
+""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
+
+
+class Chart3D(OpenChart, GeneralMethods, TitleMethods, ChartWithXYAxis, ChartWithSeries):
     """Chart for visualizing data in a 3-dimensional scene, with camera and light source(s)."""
 
     def __init__(
             self,
-            instance: Instance,
-            theme: str = 'darkGold',
+            theme: str = None,
             title: str = None
     ):
+        """Create a 3D Chart.
+
+        Args:
+            theme (str): "darkGold" | "light" | "flatDark" | "flatLight" | "lightNature" |
+                "cyberSpace" | "turquoiseHexagon"
+            title (str): A title for the chart.
+
+        Returns:
+            Reference to 3D Chart class.
+        """
+        instance = lightningchart.Instance()
         ChartWithSeries.__init__(self, instance)
         ChartWithXYAxis.__init__(self)
         self.default_z_axis = DefaultAxis(self, 'z')
         self.instance.send_command(self.id, 'chart3D', {'theme': theme})
         if title:
             self.instance.send_command(self.id, 'setTitle', {'title': title})
 
@@ -1173,24 +1302,35 @@
     def __str__(self):
         return '3D Chart'
 
 
 """"""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
 
 
-class Dashboard(GeneralMethods):
+class Dashboard(OpenChart, GeneralMethods):
     """Dashboard is a tool for rendering multiple charts in the same view."""
 
     def __init__(
             self,
-            instance: Instance,
             columns: int,
             rows: int,
-            theme: str = 'darkGold'
+            theme: str = None
     ):
+        """Create a dashboard, i.e., a tool for rendering multiple charts in the same view.
+
+        Args:
+            columns (int): The amount of columns in the dashboard.
+            rows (int): The amount of rows in the dashboard.
+            theme (str): "darkGold" | "light" | "flatDark" | "flatLight" | "lightNature" |
+                "cyberSpace" | "turquoiseHexagon"
+
+        Returns:
+            Reference to Dashboard class.
+        """
+        instance = lightningchart.Instance()
         Chart.__init__(self, instance)
         self.charts = []
         self.columns = columns
         self.rows = rows
         instance.send_command(self.id, 'dashboard', {
             'columns': columns,
             'rows': rows,
@@ -1393,19 +1533,32 @@
             'axis': axis
         })
 
 
 """"""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
 
 
-class PieChart(GeneralMethods, TitleMethods, ChartWithLUT, ChartWithLabelStyling):
+class PieChart(OpenChart, GeneralMethods, TitleMethods, ChartWithLUT, ChartWithLabelStyling):
+
+    def __init__(self, theme: str = None, labels_inside_slices: bool = False):
+        """Visualizes proportions and percentages between categories, by dividing a circle into proportional segments.
+
+        Args:
+            labels_inside_slices (bool): If true, the labels are inside pie slices. If false, the labels are on the
+                sides of the slices.
 
-    def __init__(self, instance: Instance, labels_inside_slices: bool = False):
+        Returns:
+            Reference to Pie Chart class.
+        """
+        instance = lightningchart.Instance()
         Chart.__init__(self, instance)
-        self.instance.send_command(self.id, 'pieChart', {'labelsInsideSlices': labels_inside_slices})
+        self.instance.send_command(self.id, 'pieChart', {
+            'theme': theme,
+            'labelsInsideSlices': labels_inside_slices
+        })
 
     def add_slice(self, name: str, value: int | float):
         """Add new Slice to the Pie Chart.
 
         Args:
             name (str): Initial name for Slice as string.
             value (int): Initial value for Slice as number.
@@ -1480,19 +1633,25 @@
             'rowspan': rowspan
         })
 
 
 """"""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
 
 
-class GaugeChart(GeneralMethods, TitleMethods, ChartWithLUT):
+class GaugeChart(OpenChart, GeneralMethods, TitleMethods, ChartWithLUT):
 
-    def __init__(self, instance: Instance):
+    def __init__(self, theme: str = None):
+        """A Gauge Chart with a single solid colored slice.
+
+        Returns:
+            Reference to Gauge Chart class.
+        """
+        instance = lightningchart.Instance()
         Chart.__init__(self, instance)
-        self.instance.send_command(self.id, 'gaugeChart', {})
+        self.instance.send_command(self.id, 'gaugeChart', {'theme': theme})
 
     def set_angle_interval(self, start: int | float, end: int | float):
         """Set angular interval of the gauge in degrees.
 
         Args:
             start (int | float): Start angle of the gauge in degrees.
             end (int | float): Start angle of the gauge in degrees.
@@ -1655,19 +1814,25 @@
             'rowspan': rowspan
         })
 
 
 """"""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
 
 
-class FunnelChart(GeneralMethods, TitleMethods, ChartWithLUT, ChartWithLabelStyling):
+class FunnelChart(OpenChart, GeneralMethods, TitleMethods, ChartWithLUT, ChartWithLabelStyling):
 
-    def __init__(self, instance: Instance):
+    def __init__(self, theme: str = None):
+        """Visualizes proportions and percentages between categories, by dividing a funnel into proportional segments.
+
+        Returns:
+            Reference to Funnel Chart class.
+        """
+        instance = lightningchart.Instance()
         Chart.__init__(self, instance)
-        self.instance.send_command(self.id, 'funnelChart', {})
+        self.instance.send_command(self.id, 'funnelChart', {'theme': theme})
 
     def add_slice(self, name: str, value: int | float):
         """This method is used for the adding slices in the funnel chart.
 
         Args:
             name (str): Funnel slice title.
             value (int | float): Funnel slice value.
@@ -1777,19 +1942,25 @@
             'rowspan': rowspan
         })
 
 
 """"""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
 
 
-class PyramidChart(GeneralMethods, TitleMethods, ChartWithLUT, ChartWithLabelStyling):
+class PyramidChart(OpenChart, GeneralMethods, TitleMethods, ChartWithLUT, ChartWithLabelStyling):
 
-    def __init__(self, instance: Instance):
+    def __init__(self, theme: str = None):
+        """Visualizes proportions and percentages between categories, by dividing a pyramid into proportional segments.
+
+        Returns:
+            Reference to Pyramid Chart class.
+        """
+        instance = lightningchart.Instance()
         Chart.__init__(self, instance)
-        self.instance.send_command(self.id, 'pyramidChart', {})
+        self.instance.send_command(self.id, 'pyramidChart', {'theme': theme})
 
     def add_slice(self, name: str, value: int | float):
         """This method is used for the adding slices in the pyramid chart.
 
         Args:
             name (str): Pyramid slice title.
             value (int | flaot): Pyramid slice value.
```

## lightningchart/instance.py

```diff
@@ -65,89 +65,89 @@
             try:
                 response = requests.post(f'{LOCALHOST}{server_port}/clear?id={self.id}', data=binary_data)
                 if response.ok:
                     return self
             except requests.exceptions.ConnectionError as e:
                 print(e)
 
-    def Dashboard(self, columns: int, rows: int, theme: str = 'darkGold') -> Dashboard:
+    def Dashboard(self, columns: int, rows: int, theme: str = None) -> Dashboard:
         """Create a dashboard, i.e., a tool for rendering multiple charts in the same view.
 
         Args:
             columns (int): The amount of columns in the dashboard.
             rows (int): The amount of rows in the dashboard.
             theme (str): "darkGold" | "light" | "flatDark" | "flatLight" | "lightNature" |
                 "cyberSpace" | "turquoiseHexagon"
 
         Returns:
             Reference to Dashboard class.
         """
         return Dashboard(self, columns, rows, theme)
 
-    def ChartXY(self, theme: str = 'darkGold', title: str = None) -> ChartXY:
+    def ChartXY(self, theme: str = None, title: str = None) -> ChartXY:
         """Create a XY Chart.
 
         Args:
             theme (str): "darkGold" | "light" | "flatDark" | "flatLight" | "lightNature" |
                 "cyberSpace" | "turquoiseHexagon"
             title (str): A title for the chart.
 
         Returns:
             Reference to XY Chart class.
         """
         return ChartXY(self, theme, title)
 
-    def Chart3D(self, theme: str = 'darkGold', title: str = None) -> Chart3D:
+    def Chart3D(self, theme: str = None, title: str = None) -> Chart3D:
         """Create a 3D Chart.
 
         Args:
             theme (str): "darkGold" | "light" | "flatDark" | "flatLight" | "lightNature" |
                 "cyberSpace" | "turquoiseHexagon"
             title (str): A title for the chart.
 
         Returns:
             Reference to 3D Chart class.
         """
         return Chart3D(self, theme, title)
 
-    def PieChart(self, labels_inside_slices: bool = False):
+    def PieChart(self, theme: str = None, labels_inside_slices: bool = False):
         """Visualizes proportions and percentages between categories, by dividing a circle into proportional segments.
 
         Args:
             labels_inside_slices (bool): If true, the labels are inside pie slices. If false, the labels are on the
                 sides of the slices.
 
         Returns:
             Reference to Pie Chart class.
         """
-        return PieChart(self, labels_inside_slices)
+        return PieChart(self, theme, labels_inside_slices)
 
-    def GaugeChart(self):
+    def GaugeChart(self, theme: str = None):
         """A Gauge Chart with a single solid colored slice.
 
         Returns:
             Reference to Gauge Chart class.
         """
-        return GaugeChart(self)
+        return GaugeChart(self, theme)
 
-    def FunnelChart(self):
+    def FunnelChart(self, theme: str = None):
         """Visualizes proportions and percentages between categories, by dividing a funnel into proportional segments.
 
         Returns:
             Reference to Funnel Chart class.
         """
-        return FunnelChart(self)
+        return FunnelChart(self, theme)
 
-    def PyramidChart(self):
+    def PyramidChart(self, theme: str = None):
         """Visualizes proportions and percentages between categories, by dividing a pyramid into proportional segments.
 
         Returns:
             Reference to Pyramid Chart class.
         """
-        return PyramidChart(self)
+        return PyramidChart(self, theme)
 
     def open(self, method: str = None, width: int = 800, height: int = 600):
         """Open the rendering view, either in a new browser tab or an IFrame component for Jupyter Notebook.
 
         Args:
             method (str): "browser" | "notebook", default value depends on your runtime environment.
             width (int): The width of the IFrame component in pixels.
```

## lightningchart/static/functions.js

### js-beautify {}

```diff
@@ -127,14 +127,15 @@
     setGaugeStrokeStyle: setGaugeStrokeStyle,
     setLabelEffect: setLabelEffect,
     setSliceEffect: setSliceEffect,
     addDataXY: addDataXY,
     addDataXYZ: addDataXYZ,
 }
 
+
 function addDataXY(id, x, y, clear) {
     if (clear)
         objects[id].clear();
 
     const data = [];
     for (let i = 0; i < Math.min(x.length, y.length); i++) {
         data.push({
@@ -271,36 +272,44 @@
         columnIndex: column,
         rowIndex: row,
         columnSpan: colSpan,
         rowSpan: rowSpan
     });
 }
 
-function pyramidChart(id) {
-    objects[id] = lightningChart().Pyramid();
+function pyramidChart(id, theme) {
+    theme = getTheme(theme);
+    objects[id] = lightningChart().Pyramid({
+        theme: theme
+    });
 }
 
-function funnelChart(id) {
-    objects[id] = lightningChart().Funnel();
+function funnelChart(id, theme) {
+    theme = getTheme(theme);
+    objects[id] = lightningChart().Funnel({
+        theme: theme
+    });
 }
 
 function setGaugeValue(id, value) {
     objects[id].getDefaultSlice().setValue(value);
 }
 
 function setGaugeInterval(id, start, end) {
     objects[id].getDefaultSlice().setInterval(start, end);
 }
 
 function setAngleInterval(id, start, end) {
     objects[id].setAngleInterval(start, end);
 }
 
-function gaugeChart(id) {
+function gaugeChart(id, theme) {
+    theme = getTheme(theme);
     objects[id] = lightningChart().Gauge({
+        theme: theme,
         type: lcjs.GaugeChartTypes.Solid
     });
 }
 
 function setInnerRadius(id, radius) {
     objects[id].setInnerRadius(radius);
 }
@@ -337,41 +346,64 @@
     objects[id].addSlices(slices);
 }
 
 function addSlice(id, name, value) {
     objects[id].addSlice(name, value);
 }
 
-function pieChart(id, labelsInsideSlices) {
-    if (labelsInsideSlices)
+function pieChart(id, theme, labelsInsideSlices) {
+    theme = getTheme(theme);
+    if (labelsInsideSlices) {
         objects[id] = lightningChart().Pie({
+            theme: theme,
             type: lcjs.PieChartTypes.LabelsInsideSlices
         });
-    else
+    } else {
         objects[id] = lightningChart().Pie({
+            theme: theme,
             type: lcjs.PieChartTypes.LabelsOnSides
         });
+    }
 }
 
 function getTheme(theme) {
+    if (!theme)
+        theme = ""
     switch (theme.toLowerCase()) {
         case 'light':
             return lcjs.Themes.light;
         case 'lightnature':
             return lcjs.Themes.lightNature;
         case 'cyberspace':
             return lcjs.Themes.cyberSpace;
         case 'turquoisehexagon':
             return lcjs.Themes.turquoiseHexagon;
         case 'flatdark':
             return lcjsThemes.flatThemeDark;
         case 'flatlight':
             return lcjsThemes.flatThemeLight;
-        default:
+        case 'darkgold':
             return lcjs.Themes.darkGold;
+        default:
+            const defaultTheme = lcjsThemes.makeFlatTheme({
+                isDark: false,
+                fontFamily: "Segoe UI, -apple-system, Verdana, Helvetica",
+                backgroundColor: lcjs.ColorHEX("#f8f1f1ff"),
+                textColor: lcjs.ColorHEX("#000000ff"),
+                dataColors: [lcjs.ColorHEX("#0080c0"), lcjs.ColorHEX("#ff5151"), lcjs.ColorHEX("#7dff7d"),
+                    lcjs.ColorHEX("#f9f900"), lcjs.ColorHEX("#ff94b8"), lcjs.ColorHEX("#db94c6"),
+                    lcjs.ColorHEX("#ebc4e0"), lcjs.ColorHEX("#a994c6"), lcjs.ColorHEX("#94e2c6")
+                ],
+                axisColor: lcjs.ColorHEX("#00000000"),
+                gridLineColor: lcjs.ColorHEX("#808080ff"),
+                uiBackgroundColor: lcjs.ColorHEX("#fafafaff"),
+                uiBorderColor: lcjs.ColorHEX("#000000ff"),
+                dashboardSplitterColor: lcjs.ColorHEX("#808080ff"),
+            })
+            return defaultTheme
     }
 }
 
 function sleep(ms) {
     return new Promise(resolve => setTimeout(resolve, ms));
 }
```

## Comparing `lightningchart-0.3.6.dist-info/METADATA` & `lightningchart-0.3.7.dist-info/METADATA`

 * *Files 14% similar despite different names*

```diff
@@ -1,12 +1,11 @@
 Metadata-Version: 2.1
 Name: lightningchart
-Version: 0.3.6
+Version: 0.3.7
 Home-page: 
-License-File: LICENSE
 Requires-Dist: dnspython (==2.2.1)
 Requires-Dist: eventlet (==0.33.1)
 Requires-Dist: Flask (==2.2.2)
 Requires-Dist: Flask-SocketIO (==5.3.2)
 Requires-Dist: ipython (==8.10.0)
 Requires-Dist: msgpack (==1.0.5)
 Requires-Dist: openpyxl (==3.0.10)
```

## Comparing `lightningchart-0.3.6.dist-info/RECORD` & `lightningchart-0.3.7.dist-info/RECORD`

 * *Files 7% similar despite different names*

```diff
@@ -1,16 +1,16 @@
-lightningchart/__init__.py,sha256=jeVXHk2CzSGVo6eDfn9ZbQGyFmiAZ3G-Za4RTqcGIfs,1542
-lightningchart/charts.py,sha256=mi7kNFCjbkXL8P8xnuwAy03pzf4n6UQF1mTs1VfRA4A,73241
+lightningchart/__init__.py,sha256=K9LF1c9OPqzxu0McFcVdyrmbXriNodWoKTnjg-etJak,1671
+lightningchart/charts.py,sha256=31aSrKckCIoGgO-TpttKkcbcwuVzoHPQysqh5zsaI5c,79560
 lightningchart/globals.py,sha256=2LDBVik0bUjQQyltjtHBish8ytSsv_az7PHUtsoZEg8,54
-lightningchart/instance.py,sha256=jN7oQ_uxiEgxk7O3uVyoHoAIXrNSayEP7na1ociUPgg,8507
+lightningchart/instance.py,sha256=Gbl1RgPwBXIKtrQMzTILfjscO7_XWczW_EucNZw8WeU,8593
 lightningchart/series.py,sha256=0soOG5X2IacxVLJJ-l7jC1W4BkW3fRyAZT7lAqK2bDk,49926
 lightningchart/server.py,sha256=uJErbpEMKrz-oESsjdsHcauxyxpFLnXhYFa7EHsirjY,5369
 lightningchart/ui_element.py,sha256=oVH_xleVv6uxb_dGYCMfbhG5kQNDUrDgnCSC_qdmH60,10116
 lightningchart/utils.py,sha256=p1CP9DajIa7GZDZm1ouDNOkXPAkZ5HM8MBU0WRwuZv8,12577
-lightningchart/static/functions.js,sha256=b70pB0EjpDNiETQx96hhqi4GiCM_n3kg94VMpti8GyI,34050
+lightningchart/static/functions.js,sha256=LaVF-ahoxcWN3n577doX3H7BCdWfuj4SdM6xMo8VF2U,35397
 lightningchart/static/lcjs-themes.iife.js,sha256=sGSX-OB4pPQoDYobNR5W1zUhrK0ySd85B5aUyXJWt3U,24307
 lightningchart/static/lcjs.iife.js,sha256=I_rAzBSkA3wuVrObUiJeV9e8n8XXzXffP7MgiERTtMc,1325425
 lightningchart/static/msgpack.min.js,sha256=vb5M9t5algC1c1qyuduN8oBBNinKTKTvaut6-0OPbyA,7397
 lightningchart/static/socket.io.min.js,sha256=BAHeM3AfHK0W7PlSiZ0jmQtkN9CltzNVJO32vfuTJUI,45806
 lightningchart/static/websocket.js,sha256=HG1HyNfmFkLNUucl1EgGc5ozlWwPmVG8YLFhhEvzav8,2129
 lightningchart/static/resources/maps/countries_africa.json,sha256=-TXsFi3WUMU7dhnH-zN2BbQcEFo1kXZp7j5VdrAk7bE,470264
 lightningchart/static/resources/maps/countries_asia.json,sha256=UUhsugwQziCBK6-AjuH_TzSgxzWrg9eROLVDsEWOjc0,1852822
@@ -22,13 +22,12 @@
 lightningchart/static/resources/maps/territoriesProvinces_canada.json,sha256=vL7dyK0B7Ittn9GORyZsJpXMOUrVCxVFU6boIWO2Aac,1036787
 lightningchart/static/resources/maps/territories_australia.json,sha256=poAXT9KreB5iX0rBqV4ZIFhFf8qh5tPEpI6GdvkKqP0,237008
 lightningchart/static/resources/osm/osm-sprite-transparent.png,sha256=By3HoLn3W-dZfD2odG0tOntUK20kBFZrsSbiQPous2w,4809
 lightningchart/static/resources/themes/LICENSE,sha256=3Q4CIP5k9MmWCvLix4PGRYdhxmy7NENF0geqhg4bmWI,1246
 lightningchart/static/resources/themes/cyberSpace.jpg,sha256=UC6y-4h54Ppr064z_SLkPCr3wGbFkzGnVYhA1hrclg0,442543
 lightningchart/static/resources/themes/turquoiseHexagon.jpg,sha256=iHjRlpQx6FZ3qT9IWjHSWHKA14jgfT4HF4fg4ce2FFQ,495667
 lightningchart/templates/index.html,sha256=3mFp2cPE2p5wXA7k4f4yiZK0GX584_oxBO5sBMAJzUY,1347
-lightningchart-0.3.6.dist-info/LICENSE,sha256=GAYahFipxQAZViWbd7qkDBFB8ED-O9wxCjFswxQ4uTg,1094
-lightningchart-0.3.6.dist-info/METADATA,sha256=6uLn6DiupGFwSKmVLDAg-XZnx_TWrfMV1X6af7DHcNE,551
-lightningchart-0.3.6.dist-info/WHEEL,sha256=G16H4A3IeoQmnOrYV4ueZGKSjhipXx8zc8nu9FGlvMA,92
-lightningchart-0.3.6.dist-info/entry_points.txt,sha256=9oEyJVjew5BaamK79WdgwkrkCxUHFoi20z0Xg67cOog,62
-lightningchart-0.3.6.dist-info/top_level.txt,sha256=H7WlInBfCT1IP3iECm-FHT_2EKh6-jNI03xN2SYNink,15
-lightningchart-0.3.6.dist-info/RECORD,,
+lightningchart-0.3.7.dist-info/METADATA,sha256=9Fcc0NnGPXsCjol3mmhxyg6bpW7v7FdsYxyuixZI698,529
+lightningchart-0.3.7.dist-info/WHEEL,sha256=G16H4A3IeoQmnOrYV4ueZGKSjhipXx8zc8nu9FGlvMA,92
+lightningchart-0.3.7.dist-info/entry_points.txt,sha256=9oEyJVjew5BaamK79WdgwkrkCxUHFoi20z0Xg67cOog,62
+lightningchart-0.3.7.dist-info/top_level.txt,sha256=H7WlInBfCT1IP3iECm-FHT_2EKh6-jNI03xN2SYNink,15
+lightningchart-0.3.7.dist-info/RECORD,,
```

